# 智慧医疗住院数据分析平台 Next.js 前端编写计划

## 1. 目标与范围

本阶段使用 Next.js 实现医疗住院数据分析 BI 大屏，消费已完成的 Flask 接口，展示患者结构、医院运营和疾病分析结果。页面以 `ChatGPT Image 2026年8月15日 16_34_04.png` 为布局和视觉参考，不直接复制图片中的示例数据。

本阶段不实现 AI Agent 聊天入口、住院明细追溯页面和用户权限管理；优先完成一张可筛选、可下钻、可联调的大屏。

## 2. 视觉方向

视觉方向定位为“临床运营指挥台”：深色背景、高信息密度、克制的发光边框和明确的层级，不做营销页或大面积装饰。

| 项目 | 计划 |
|---|---|
| 背景 | 近黑蓝背景，叠加低对比度网格纹理，突出数据面板。 |
| 强调色 | 青蓝用于边框与交互状态，蓝色用于主要数据，橙色用于女性/告警/成本强调，绿色用于低风险或正向指标。 |
| 字体 | 中文使用 `Noto Sans SC` 或思源黑体；数值使用等宽字体，方便扫描和对比。 |
| 面板 | 直角或轻微圆角，细描边，避免卡片套卡片；标题使用左侧短竖线或图标作为定位标识。 |
| 图表 | 使用 ECharts，统一深色主题、提示框、图例和数字格式。 |
| 动效 | 初次加载分区渐入；筛选变化时局部骨架屏；图表 hover 高亮，不添加影响阅读的持续动画。 |

## 3. 技术方案

### 3.1 前端技术栈

```text
Next.js（App Router）
TypeScript
React
ECharts
CSS Modules 或 Tailwind CSS
Lucide React 图标
```

ECharts 图表组件必须以 Client Component 方式加载，避免服务端渲染访问浏览器 DOM。使用 `dynamic(..., { ssr: false })` 或统一的 `EChart` 客户端包装组件。

### 3.2 与 Flask 的连接

开发环境中，Next.js 通过环境变量读取 Flask 地址：

```text
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:5000/api
```

所有请求统一由 `lib/api-client.ts` 发出，负责：

```text
拼接查询参数
检查 HTTP 状态码和 API code
解析 JSON
处理空数据和错误信息
```

## 4. 推荐目录结构

```text
web/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── components/
│   ├── dashboard/
│   │   ├── DashboardShell.tsx
│   │   ├── DashboardHeader.tsx
│   │   ├── GlobalFilterBar.tsx
│   │   ├── PatientPanel.tsx
│   │   ├── HospitalPanel.tsx
│   │   └── DiseasePanel.tsx
│   ├── charts/
│   │   ├── EChart.tsx
│   │   ├── AgeGenderChart.tsx
│   │   ├── PaymentChart.tsx
│   │   ├── DispositionChart.tsx
│   │   ├── AdmissionEmergencyChart.tsx
│   │   ├── HospitalBubbleChart.tsx
│   │   ├── HospitalRankingChart.tsx
│   │   ├── DiseaseSystemChart.tsx
│   │   ├── TopDiagnosisChart.tsx
│   │   └── SeverityChart.tsx
│   ├── hospital/
│   │   ├── KpiCard.tsx
│   │   └── HospitalComparisonTable.tsx
│   ├── disease/
│   │   └── DiseaseDrilldown.tsx
│   └── ui/
│       ├── Panel.tsx
│       ├── PanelTitle.tsx
│       ├── LoadingState.tsx
│       ├── EmptyState.tsx
│       └── ErrorState.tsx
├── hooks/
│   ├── useDashboardFilters.ts
│   ├── useDashboardData.ts
│   └── useEChartResize.ts
├── lib/
│   ├── api-client.ts
│   ├── dashboard-api.ts
│   ├── formatters.ts
│   └── chart-theme.ts
├── types/
│   └── dashboard.ts
└── .env.local
```

## 5. 页面布局方案

### 5.1 桌面端布局

页面使用顶部栏加三列内容区：

```text
┌───────────────────────────────────────────────────────────────┐
│ 医疗图标｜智慧医疗住院数据分析平台｜更新时间                   │
│ 服务区域｜医院所在县｜医疗机构名称                             │
├──────────────┬──────────────────────────────┬─────────────────┤
│ 患者分析      │ 医院运营分析                 │ 疾病与风险      │
│ 4 个图表      │ KPI + 气泡图 + 排名 + 对比表 │ 4 个图表/下钻   │
└──────────────┴──────────────────────────────┴─────────────────┘
```

推荐 Grid 比例：

```css
grid-template-columns: minmax(300px, 1fr) minmax(620px, 1.9fr) minmax(320px, 1fr);
```

中心列为视觉和业务重点，KPI 占据最上方；左、右两列保持图表纵向等距排列。医院对比表固定表头、内容区内部滚动，不让整页因为表格行数改变高度。

### 5.2 响应式策略

| 视口 | 布局 |
|---|---|
| `>= 1440px` | 三列完整大屏布局。 |
| `1024px - 1439px` | 中部优先，左右栏目变为两列网格或缩窄列宽。 |
| `< 1024px` | 顶部筛选纵向排列，患者、医院、疾病三个面板按顺序单列展示。 |

图表容器必须具有稳定高度；使用 `ResizeObserver` 调用 ECharts `resize()`，防止筛选或窗口变化导致画布空白。

## 6. 全局状态与数据请求

### 6.1 筛选状态

全局状态仅保存三个字段：

```ts
type DashboardFilters = {
  hospital_service_area?: string;
  hospital_county?: string;
  facility_name?: string;
};
```

交互规则：

1. 页面加载时请求 `/filters/areas`。
2. 选择区域后清空县和医院，请求 `/filters/counties`。
3. 选择县后清空医院，请求 `/filters/facilities`。
4. 任一筛选变化后，所有大屏模块携带同一筛选参数并行重新请求。
5. 使用 `AbortController` 取消旧请求，防止快速切换筛选时旧响应覆盖新状态。

### 6.2 通用数据状态

每个模块统一处理四种状态：

```text
loading：显示局部骨架屏，不遮挡其他已完成模块。
success：渲染图表或表格。
empty：显示“当前筛选条件下暂无数据”。
error：显示简洁错误状态和重试按钮。
```

## 7. 模块拆分与接口映射

### 7.1 大屏框架与筛选模块

| 前端组件 | 接口 | 功能 |
|---|---|---|
| `DashboardHeader` | 无 | 标题、医疗图标、全局更新时间。 |
| `GlobalFilterBar` | `/filters/areas`、`/filters/counties`、`/filters/facilities` | 三级级联筛选和重置操作。 |
| `DashboardShell` | 各模块 `updated_at` | 统一布局，收集并显示最新更新时间。 |

### 7.2 左侧患者分析模块

| 组件 | 接口 | 图表与字段 |
|---|---|---|
| `AgeGenderChart` | `/dashboard/patient/age-gender` | 年龄组为横轴、性别为堆叠系列，按 `age_group_sort` 排序。 |
| `PaymentChart` | `/dashboard/patient/payment` | 环形图显示 `payment_typology_1` 和 `discharge_ratio`，中心显示总出院量。 |
| `DispositionChart` | `/dashboard/patient/disposition?limit=10` | 横向条形图，按 `discharge_count` 降序。 |
| `AdmissionEmergencyChart` | `/dashboard/patient/admission-emergency` | 按入院类型和急诊标记的堆叠柱状图。 |

### 7.3 中部医院运营模块

| 组件 | 接口 | 图表与字段 |
|---|---|---|
| `KpiCard` 组 | `/dashboard/kpi` | 医院数、出院量、平均住院天数、总收费、总成本、平均收费、平均成本、急诊占比。 |
| 急诊成本对比 | `/dashboard/kpi` | 使用急诊/非急诊收费与成本，可使用细型双组柱状图或在 KPI 提示层显示。 |
| `HospitalBubbleChart` | `/dashboard/hospital/resources` | X 为出院量，Y 为平均成本，气泡大小为出院量，颜色为平均住院天数。 |
| `HospitalRankingChart` | `/dashboard/hospital/ranking` | 排序下拉框控制 `sort_by`、`order`、`limit`。 |
| `HospitalComparisonTable` | `/dashboard/hospital/comparison` | 服务端分页、排序、医院名称搜索。 |

### 7.4 右侧疾病与严重程度模块

| 组件 | 接口 | 图表与字段 |
|---|---|---|
| `DiseaseSystemChart` | `/dashboard/disease/systems` | APR MDC 疾病系统柱状图；标签显示“编码 - 描述”。 |
| `TopDiagnosisChart` | `/dashboard/disease/top-diagnoses?limit=10` | CCSR 高发疾病横向条形图。 |
| `SeverityChart` | `/dashboard/disease/risk` | 仅展示 Minor、Moderate、Major、Extreme 四类病情严重程度柱状图。 |
| `DiseaseDrilldown` | `/dashboard/disease/drilldown` | MDC → DRG → CCSR 三级卡片下钻与面包屑返回。 |

## 8. 图表实现细节

### 8.1 ECharts 通用规范

```text
颜色：蓝 #3B82F6、青 #22D3EE、橙 #F59E0B、绿 #34D399、红 #F87171。
坐标轴：低对比度灰蓝细线，文字不倾斜时优先不倾斜。
提示框：深色实底，高对比度数值，展示格式化后的金额和百分比。
图例：放置在标题右侧或图表顶部，不占用数据绘制区域。
数据标签：仅在条形图和严重程度柱状图显示，避免密集图表全部贴标签。
```

金额格式：`$150.58B`、`$45.11B` 或按中文页面统一为 `1,505.84 亿元`；页面内必须统一一种口径。住院量显示整数千分位，比例显示为百分比，平均住院天数保留两位小数。

### 8.2 疾病下钻规则

1. 初始请求：`/dashboard/disease/drilldown?level=mdc`。
2. 点击 MDC：请求 `level=drg&apr_mdc_code=<code>`。
3. 点击 DRG：请求 `level=ccsr&apr_mdc_code=<code>&apr_drg_code=<code>`。
4. 点击面包屑：回到对应层级并清除更深层状态。
5. 下钻请求始终携带三个全局筛选字段。

## 9. 类型定义计划

`types/dashboard.ts` 先定义公共接口响应、筛选条件和所有接口返回项，重点类型包括：

```text
ApiResponse<T>
DashboardFilters
AgeGenderItem
PaymentItem
DispositionItem
AdmissionEmergencyItem
KpiData
HospitalResourceItem
HospitalRankingItem
HospitalComparisonPage
DiseaseSystemItem
DiagnosisItem
SeverityItem
DiseaseDrilldownItem
```

前端禁止把接口返回值直接声明为 `any`；图表数据转换放在组件内部或专门的 mapper 函数中。

## 10. 实施顺序

1. 初始化 Next.js TypeScript 项目，配置 `.env.local`、基础字体、全局颜色变量和页面容器。
2. 编写 `api-client.ts`、类型定义、金额/比例格式化函数和三级筛选状态 Hook。
3. 实现大屏骨架、顶部栏、筛选栏和通用 `Panel`、加载/空/错误状态组件。
4. 接入左侧四个患者图表，验证筛选联动和 ECharts 自适应。
5. 接入中部 KPI、气泡图、医院排名和对比表，完成分页、排序和搜索。
6. 接入右侧疾病系统、高发疾病、病情严重程度柱状图和疾病下钻。
7. 优化 1920×1080、1440×900、1280×800、1024px 以下视口；检查文字截断、图表溢出和表格滚动。
8. 联调 Flask 错误和空数据响应，补充加载状态、重试、筛选重置和请求取消。
9. 使用浏览器截图检查视觉对齐、图例可读性、深色主题对比度和交互状态。

## 11. 验收标准

| 项目 | 验收要求 |
|---|---|
| 布局 | 1920×1080 下保持左-中-右三栏，中心医院运营区最突出。 |
| 筛选 | 区域、县、医院三级联动，筛选变化后全部模块同步更新。 |
| 数据 | 9 张 Spark 汇总表对应的接口均能正确渲染；无数据、加载失败时界面不崩溃。 |
| 图表 | 所有 ECharts 容器缩放正常，无重叠、无空白画布、无文字越界。 |
| 下钻 | MDC → DRG → CCSR 支持逐层进入和面包屑返回，保留全局筛选。 |
| 表格 | 医院对比表支持服务端分页、排序和名称搜索。 |
| 风格 | 视觉接近参考图的医疗数据大屏结构与深色高密度风格，但使用真实接口数据和当前业务名称。 |

## 12. 实施记录（2026-08-15）

- [x] 已创建 `web` Next.js App Router + TypeScript 项目，并安装 `echarts`、`lucide-react`。
- [x] 已配置 `.env.local`，前端通过 `NEXT_PUBLIC_API_BASE_URL` 连接 Flask `/api`。
- [x] 已完成 API 客户端、接口 TypeScript 类型、金额/比例格式化工具和三级筛选状态。
- [x] 已完成参考图风格的深色医疗驾驶舱页面：顶部栏、筛选栏、左中右三栏、细线面板、KPI 卡片和响应式断点。
- [x] 已接入左侧患者分析四个图表：年龄与性别、支付方式、离院去向、入院与急诊。
- [x] 已接入中部医院运营模块：KPI、资源成本气泡图、运营排名、分页医院对比表。
- [x] 已接入右侧疾病模块：疾病系统、高发疾病、病情严重程度柱状图和 MDC → DRG → CCSR 下钻。
- [x] 已实现加载、空数据、接口错误、筛选重置、请求取消、医院排名切换和表格翻页。
- [x] 已执行 `npm run lint` 与 `npm run build`，均通过。
- [x] 已验证 `http://127.0.0.1:3000/` 返回 `200`，并确认其可连接 Flask KPI 接口。
- [ ] 当前环境未提供可用浏览器自动化实例，未能完成截图级视觉回归；需在可用浏览器环境下补做 1920×1080、1440×900、1280×800 和移动端截图检查。
