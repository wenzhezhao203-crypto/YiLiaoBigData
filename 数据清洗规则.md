# 医疗住院出院数据清洗规则

## 1. 处理原则

- 使用 Pandas 分块读取 CSV/TSV，不一次性载入全部数据；默认每块 50,000 行。
- 不擅自删除不能确定为错误的医疗记录。完全重复和缺少核心分析字段的记录不写入主表，但必须写入质量报告或异常记录表以便追溯。
- 所有编码字段按字符串处理，严禁先转为数值再保存，避免前导零丢失。
- 规则同时适用于首批导入和后续增量导入，所有规则执行结果均须计入数据质量报告。

## 2. 表头与字段标准化

| 原始字段 | 入库字段 | 规则 |
|---|---|---|
| `Hospital County` 或 `County` | `hospital_county` | 两种表头均接受。当前样例使用 `County`。 |
| `Permanent Facility Id` | `permanent_facility_id` | 字符串保存，例如 `001169`。 |
| `Zip Code - 3 digits` | `zip_code_3` | 字符串保存，不转换为整数。 |
| `CCSR/APR` 各类编码 | 对应 snake_case 字段 | 保留前导零，例如 `04`、`045`、`03100`。 |
| 其他字段 | 小写 snake_case 字段 | 按字段映射表统一命名。 |

`Hospital Service Area` 在部分源文件中可能不存在。该字段缺失时允许以 `NULL` 入库，不阻止整批数据导入。

## 3. 通用字符串和缺失值规则

| 项目 | 规则 |
|---|---|
| 空白字符 | 去除首尾空格；空字符串转为 `NULL`。 |
| 缺失标识 | `N/A`、`NA`、`NULL`、空白单元格统一转为 `NULL`。 |
| 有效分类值 | `Unknown`、`Other Race`、`Multi-racial`、`Not Span/Hispanic` 等保留，不视为缺失。 |
| 大小写 | 枚举字段的大小写统一；描述字段保留原始文本，不强制全部转大写。 |
| 特殊字符 | 保留诊断和操作描述中的逗号、分号、括号及连字符；CSV 读取必须遵从双引号转义。 |

## 4. 数值、金额与编码规则

| 字段 | 清洗规则 | 异常处理 |
|---|---|---|
| `Length of Stay` | 转为数值，可保留 `0` 天住院。 | 小于 0 或无法转换时置空并记录。 |
| `Discharge Year` | 转为整数。 | 不在 `2000` 至当前年份范围内时置空并记录。 |
| `APR Severity of Illness Code` | 转为整数。 | 仅接受 1-4，其他值标记异常。 |
| `APR Risk of Mortality` | 转为整数。 | 仅接受 1-4，其他值标记异常。 |
| `Total Charges`、`Total Costs` | 去除千位逗号和货币符号，转为 `DECIMAL(16,2)`。 | 负数或无法转换时置空并记录。 |
| `Birth Weight` | 将 `02000`、`03100`、`04800` 等零填充值转为数值 2000、3100、4800。 | 小于等于 0 或明显超出生理合理范围的值标记异常，不直接删除病例。 |
| CCSR、DRG、MDC、医院 ID、ZIP3 | 一律作为字符串。 | 不做数值转换，防止 `001169`、`045` 等前导零丢失。 |

`Total Costs` 高于 `Total Charges`、费用显著偏高或住院天数显著偏长，可能是复杂病例而非录入错误。因此仅增加质量标记，后续可按年度、疾病或医院计算 IQR/Z-score 异常标记，不直接修改原值。

## 5. 枚举字段规范化

| 字段 | 合法或常见值 | 处理规则 |
|---|---|---|
| `Age Group` | `0 to 17`、`18 to 29`、`30 to 49`、`50 to 69`、`70 or Older` | 保留原值并建立排序序号；未知值标记而不删除。 |
| `Gender` | `M`、`F` | 将 `Male/Female` 标准化为 `M/F`；`Unknown` 或其他有效来源值保留并标记。 |
| `Type of Admission` | `Emergency`、`Urgent`、`Elective`、`Newborn` | 统一大小写；未在此集合中的值保留并计入质量报告。 |
| `Emergency Department Indicator` | `Y`、`N` | `Y/Yes/True/1 -> Y`，`N/No/False/0 -> N`，其他值置空并记录。 |
| `APR Medical Surgical Description` | `Medical`、`Surgical` | 统一大小写；未知值标记。 |
| `Patient Disposition` | 如 `Home or Self Care`、`Home w/ Home Health Services`、`Skilled Nursing Home`、`Hospice - Medical Facility`、`Left Against Medical Advice`、`Expired` | 保留原始离院去向；同时可生成标准分组字段。`Expired` 生成 `is_in_hospital_death = 1`，不删除记录。 |
| 支付方式 | Medicare、Medicaid、Private Health Insurance、Self-Pay 等 | 支付方式 1、2、3 独立保留；空的次要/第三支付方式属于合法缺失，同一支付方式重复出现在多个支付字段中也属于合法值。 |

## 6. 样例数据新增的跨字段规则

| 规则 | 样例依据 | 处理方式 |
|---|---|---|
| 非数字 ZIP3 | 出现 `OOS`。 | `OOS` 表示州外地址，保留为 `OOS`，不可强制转换为三位数字；可新增 `zip_region_type = OUT_OF_STATE`。 |
| 新生儿判定 | `Age Group = 0 to 17` 中既有 `Newborn` 也有非新生儿病例。 | 只能以 `Type of Admission = Newborn` 判断新生儿，不能仅依据年龄组。 |
| 出生体重适用范围 | 新生儿记录出现 `02000` 至 `04800`；非新生儿通常为空。 | 仅新生儿入院保留出生体重；非新生儿的出生体重置为 `NULL`。新生儿缺失出生体重只记录质量问题，不删除记录。 |
| 操作字段缺失 | 多条诊断记录没有 CCSR Procedure Code/Description。 | 手术/操作代码和描述可以同时为空，属于合法缺失；不得据此删除病例。 |
| 代码-描述配对 | 部分记录有完整 CCSR/DRG/MDC 对，部分操作对缺失。 | 诊断代码与诊断描述、DRG 代码与描述、MDC 代码与描述若只存在一方则标记 `code_description_mismatch`；不凭空补值。操作代码和操作描述必须同时为空或同时存在。 |
| 医院主数据一致性 | 同一医院永久 ID 应稳定对应机构名称和运营证书号。 | 建立医院映射校验；若一个 ID 对应多个名称或证书号，写入异常报告，人工确认后再修正。 |
| 诊断与分类不一致 | 样例中可出现 CCSR 诊断与 APR DRG/MDC 描述不直观一致。 | 不自动判断为错误或改写，以官方编码映射表校验后再标记。 |

## 7. 去重与核心字段校验

1. 当前数据未提供患者唯一 ID 或住院流水号，不能依据医院、疾病、年龄等字段做业务去重。
2. 仅对所有原始业务字段完全一致的记录去重。
3. 清洗后对整行业务字段生成 `SHA-256` 指纹，并在 MySQL 设置唯一索引，用于跨文件、跨批次增量导入去重。
4. `discharge_year`、`ccsr_diagnosis_code`、`length_of_stay` 缺失时，记录不进入主分析表。
5. 医院标识缺失时，若机构名称为 `Redacted for Confidentiality`，则判定为脱敏医院记录并在清洗阶段直接删除；该记录缺少可识别医院及位置，不纳入本项目的医院维度分析。
6. 其他字段，包括操作、种族、民族、次要支付方式、出生体重，允许为空。

## 8. 入库与质量报告

- 主表保存清洗后的可分析数据；异常记录表或 CSV 保存未入主表记录及其拒绝原因。
- 使用批量 `INSERT`/`INSERT IGNORE` 写入 MySQL，并按 `source_row_hash` 忽略重复导入。
- 每批导入生成 JSON/CSV 质量报告，至少包含：输入总行数、主表入库数、跨批次重复数、完全重复数、核心字段缺失数、各字段缺失率、数值异常数、枚举异常数、代码描述不配对数、医院映射冲突数。
- 原始文件保留只读副本；清洗后的字段、异常原因、导入时间和源文件名必须可追溯。

## 9. 第二批样例补充规则

| 规则 | 样例依据 | 处理方式 |
|---|---|---|
| 脱敏医院信息 | 出现 `Redacted for Confidentiality`，且服务区、县、运营证书号、医院永久 ID 同时为空。 | 直接删除该记录，不写入主表；质量统计中的 `discarded_redacted_facility_rows` 记录删除数量。 |
| 服务区域扩展 | 同时出现 `New York City` 与 `Long Island` 服务区域。 | 服务区域和县保留为字符串；后续如需校验，只能依据官方医院区域映射表标记不一致，不能根据名称自动改写。 |
| 支付方式重复 | 样例存在 `Medicare, Medicare`、`Medicaid, Medicaid, Self-Pay` 等组合。 | 三个支付字段保留原始顺序和重复值，不作跨字段去重。若做支付方式占比分析，只使用 `Payment Typology 1`；若做多支付方式分析，再派生去重后的支付方式集合。 |
| 离院去向细分 | 新增 `Hospice - Home`、`Inpatient Rehabilitation Facility`。 | 原始值完整保留，并派生标准分组：`HOSPICE`、`REHABILITATION`、`SKILLED_NURSING`、`HOME_HEALTH`、`HOME_SELF_CARE`、`LEFT_AGAINST_MEDICAL_ADVICE`、`EXPIRED`、`OTHER`。 |
| 极端费用和长住院 | 出现住院 67 天、费用超过 1,300,000 的病例，同时伴随 `Extreme` 严重程度和复杂操作。 | 不按固定金额或住院天数阈值删除。按 `出院年份 + 疾病大类/DRG` 计算异常分位数，仅添加 `cost_outlier_flag`、`charge_outlier_flag`、`los_outlier_flag` 供分析筛选。 |
| 金额整数形式 | `Total Charges` 可为 `463,812`，没有小数部分。 | 去除千位分隔符后仍转换为两位小数，例如 `463812.00`。 |
| 医疗分类与操作关系 | `Medical` 病例可以带有透析、导管、隔离等操作；`Surgical` 病例也可能没有操作字段。 | 不以是否存在操作字段反推内科/外科分类，不做自动修正；仅保留官方 `APR Medical Surgical Description`。 |
| 入院类型与急诊标记 | `Emergency/Urgent/Elective/Newborn` 与 `Y/N` 急诊标记并非完全等价。 | 不强制二者一致。出现 `Emergency + N` 或 `Elective + Y` 时可作为软校验记录，仍保留原始值。 |
