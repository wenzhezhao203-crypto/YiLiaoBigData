| 成员 | 核心职责 | 主要任务 | 主要交付 |
|---|---|---|---|
| 成员 1 | 数据清洗与 MySQL 入库 | 读取 CSV/TSV 原始数据；处理缺失值、异常值和重复数据；统一字段类型与编码格式；设计并创建 MySQL 数据表；批量导入清洗后的数据 | Pandas/PySpark 清洗脚本、MySQL 建表 SQL、原始数据表与清洗后明细表、数据导入说明、数据质量统计结果 |
| 成员 1 | Spark 数据分析 | 从 MySQL 读取清洗后的住院数据；完成疾病、年龄、医院、年份、费用、支付方式、住院时长等维度聚合分析；将分析结果写回 MySQL | PySpark 分析任务脚本、分析结果表 SQL、各维度统计结果、Spark 任务运行说明 |
| 成员 1-2-3-4(根据展示模块) | Flask 后端接口 | 设计并实现分析数据查询 API；连接 MySQL 查询明细和分析结果；接入 Redis 缓存热点数据；完成参数校验、异常处理和统一 JSON 响应 | Flask 项目代码、RESTful API、Redis 缓存逻辑、接口文档、接口测试结果 |
| 成员 5 | AI Agent 智能交互 | 实现自然语言意图识别；根据问题调用 Flask 分析接口；整合结构化结果并生成医疗洞察文本；生成 ECharts 图表配置；维护多轮对话上下文 | Agent 代码、工具调用配置、提示词模板、文字洞察生成模块、图表配置生成模块、多轮对话模块 |
| 成员 1 | Docker 与 Git 配置管理 | 编写并维护 Docker Compose；部署 MySQL、Redis、Spark 单机环境；统一组件版本、环境变量、端口、数据卷；维护 Git 分支、提交规范和项目文档 | `docker-compose.yml`、`.env.example`、组件版本说明、启动与停止脚本、Git 分支规范、项目部署文档、依赖清单 |