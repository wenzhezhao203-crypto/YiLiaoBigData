# 医疗住院数据平台数据库表结构说明

本文档记录当前数据库中五张核心表的字段、数据类型和业务含义。数据库为 MySQL 8，字符集为 `utf8mb4`。四张汇总表由 Spark 从 `inpatient_discharges` 聚合生成，均保留三个全局筛选字段：`hospital_service_area`、`hospital_county`、`facility_name`。

## 1. `inpatient_discharges`

住院患者出院明细主表。每行代表一条清洗后的住院出院记录，是 Spark 分析和 Flask 接口查询的基础数据表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | BIGINT UNSIGNED | 自增主键。 |
| `source_row_hash` | CHAR(64) | 清洗后业务字段的 SHA-256 指纹，用于增量去重。 |
| `source_file_name` | VARCHAR(255) | 原始数据文件名，用于数据追溯。 |
| `source_row_number` | BIGINT UNSIGNED | 原始文件中的行号，用于定位源记录。 |
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，可为空。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，可为空。 |
| `operating_certificate_number` | VARCHAR(30) | 医院运营证书编号，按字符串保留。 |
| `permanent_facility_id` | VARCHAR(30) | 医疗机构永久 ID，保留前导零。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称；脱敏记录使用占位值。 |
| `is_facility_redacted` | TINYINT(1) | 是否为脱敏医院记录：1 是，0 否。 |
| `age_group` | VARCHAR(30) | 原始年龄组，例如 `50 to 69`。 |
| `age_group_sort` | TINYINT UNSIGNED | 年龄组排序值，保证图表按年龄顺序展示。 |
| `zip_code_3` | VARCHAR(10) | 三位邮编，或 `OOS` 等特殊值。 |
| `zip_region_type` | VARCHAR(30) | 邮编类型：`LOCAL`、`OUT_OF_STATE` 或 `UNKNOWN`。 |
| `gender` | VARCHAR(30) | 标准化性别。 |
| `race` | VARCHAR(100) | 种族。 |
| `ethnicity` | VARCHAR(100) | 民族。 |
| `length_of_stay` | DECIMAL(10,2) | 住院时长，单位为天。 |
| `type_of_admission` | VARCHAR(50) | 入院类型。 |
| `emergency_department_indicator` | CHAR(1) | 是否经急诊：`Y`、`N` 或空。 |
| `patient_disposition` | VARCHAR(100) | 原始离院去向，例如居家、护理机构、转院或死亡。 |
| `disposition_group` | VARCHAR(50) | 预留的离院去向分组字段；当前不依赖映射表维护，可为空。 |
| `discharge_year` | SMALLINT UNSIGNED | 出院年份。 |
| `ccsr_diagnosis_code` | VARCHAR(30) | CCSR 诊断编码。 |
| `ccsr_diagnosis_description` | VARCHAR(500) | CCSR 诊断描述。 |
| `ccsr_procedure_code` | VARCHAR(30) | CCSR 操作编码，可为空，因为部分住院记录没有操作。 |
| `ccsr_procedure_description` | VARCHAR(500) | CCSR 操作描述，可为空。 |
| `apr_drg_code` | VARCHAR(30) | APR DRG 编码，保留前导零。 |
| `apr_drg_description` | VARCHAR(500) | APR DRG 描述。 |
| `apr_mdc_code` | VARCHAR(30) | APR MDC 编码，保留前导零。 |
| `apr_mdc_description` | VARCHAR(500) | APR MDC 疾病系统描述。 |
| `apr_severity_code` | TINYINT UNSIGNED | 病情严重程度代码，通常为 1-4。 |
| `apr_severity_description` | VARCHAR(50) | 病情严重程度描述。 |
| `apr_risk_of_mortality` | TINYINT UNSIGNED | 死亡风险等级，通常为 1-4。 |
| `apr_medical_surgical_description` | VARCHAR(50) | 内科/外科分类。 |
| `payment_typology_1` | VARCHAR(100) | 主要支付方式。 |
| `payment_typology_2` | VARCHAR(100) | 次要支付方式，可为空。 |
| `payment_typology_3` | VARCHAR(100) | 第三支付方式，可为空。 |
| `birth_weight` | DECIMAL(10,2) | 新生儿出生体重；非新生儿记录为空。 |
| `total_charges` | DECIMAL(16,2) | 医疗总收费金额。 |
| `total_costs` | DECIMAL(16,2) | 医院总成本金额。 |
| `charge_cost_gap` | DECIMAL(16,2) | 总收费减总成本的差额。 |
| `created_at` | TIMESTAMP | 记录入库时间。 |

## 2. `patient_age_gender_summary`

按区域、县、医院、年龄组和性别聚合的患者结构表，用于左侧“年龄与性别结构”图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `age_group` | 字符串 | 年龄组。 |
| `age_group_sort` | 整数 | 年龄组排序值。 |
| `gender` | 字符串 | 性别。 |
| `discharge_count` | BIGINT | 该年龄组和性别组合的出院记录数。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

分组逻辑：`GROUP BY hospital_service_area, hospital_county, facility_name, age_group, age_group_sort, gender`。

## 3. `patient_payment_summary`

按主要支付方式聚合的支付结构表，用于左侧“支付方式分布”图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `payment_typology_1` | 字符串 | 主要支付方式。 |
| `discharge_count` | BIGINT | 该支付方式对应的出院记录数。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该支付方式对应的总收费金额。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该支付方式对应的总成本金额。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

占比和平均收费等指标可由前端或 Flask 根据当前筛选结果计算：`discharge_count / 筛选总记录数`、`total_charges_sum / discharge_count`。

## 4. `patient_disposition_summary`

按原始离院去向聚合的离院结果表，用于左侧“离院去向 Top N”图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `patient_disposition` | VARCHAR(100) | 原始离院去向。 |
| `discharge_count` | BIGINT | 该离院去向对应的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该离院去向所有记录的住院天数总和。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该离院去向所有记录的总收费金额。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该离院去向所有记录的总成本金额。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

可计算平均住院天数、平均收费和平均成本：对应总和除以 `discharge_count`。前端可按 `discharge_count` 降序取 Top 10。

## 5. `patient_admission_emergency_summary`

按入院类型和急诊标记聚合的入院结构表，用于左侧“入院与急诊结构”图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `type_of_admission` | VARCHAR(50) | 入院类型。 |
| `emergency_department_indicator` | CHAR(1) | 急诊标记：`Y`、`N` 或空。 |
| `discharge_count` | BIGINT | 该入院类型和急诊组合的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该组合所有记录的住院天数总和。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

急诊占比可通过 `emergency_department_indicator = 'Y'` 的记录数除以当前筛选总记录数计算；平均住院天数为 `length_of_stay_sum / discharge_count`。

## 5.1 `patient_medical_surgical_summary`

按内科/外科分类聚合的患者诊疗结构表，用于左侧“入院与急诊结构”卡片切换后的内外科比例图。Spark 按区域、县、医院和 `apr_medical_surgical_description` 聚合生成。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，全局筛选维度。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，全局筛选维度。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称，全局筛选维度。 |
| `apr_medical_surgical_description` | VARCHAR(50) | 内科/外科分类值，例如 `Medical`、`Surgical`。 |
| `discharge_count` | BIGINT | 该内外科分类的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该分类所有记录的住院天数总和。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该分类所有记录的总收费金额。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该分类所有记录的总成本金额。 |
| `updated_at` | TIMESTAMP | Spark 汇总结果生成时间。 |

Spark 仅保留标准分类 `Medical` 和 `Surgical`，排除为空、空白或 `Not Applicable` 的明细。前端计算口径：内外科占比 = `discharge_count / SUM(discharge_count)`；平均住院天数、平均收费、平均成本分别为对应总和除以 `discharge_count`。

## 6. `hospital_operation_summary`

按区域、县和医院名称聚合的医院运营汇总表，用于 BI 大屏中部的核心运营指标、医院资源与成本、医院运营排名和医院对比表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度，也是医院对比表的展示字段。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度和医院展示名称。 |
| `discharge_count` | BIGINT | 该医院的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该医院所有记录的住院天数总和。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该医院的总收费金额。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该医院的总成本金额。 |
| `charge_cost_gap_sum` | DECIMAL/DOUBLE | 该医院总收费减总成本的差额。 |
| `emergency_charges_sum` | DECIMAL/DOUBLE | 急诊标记为 `Y` 的记录总收费金额。 |
| `non_emergency_charges_sum` | DECIMAL/DOUBLE | 急诊标记为 `N` 的记录总收费金额。 |
| `emergency_costs_sum` | DECIMAL/DOUBLE | 急诊标记为 `Y` 的记录总成本金额。 |
| `non_emergency_costs_sum` | DECIMAL/DOUBLE | 急诊标记为 `N` 的记录总成本金额。 |
| `emergency_yes_count` | BIGINT | 急诊标记为 `Y` 的出院记录数。 |
| `emergency_known_count` | BIGINT | 急诊标记为 `Y` 或 `N` 的有效记录数。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

分组逻辑：`GROUP BY hospital_service_area, hospital_county, facility_name`。

中部指标按当前全局筛选结果计算：平均住院天数为 `SUM(length_of_stay_sum) / SUM(discharge_count)`；平均收费和平均成本同理；急诊占比为 `SUM(emergency_yes_count) / SUM(emergency_known_count)`。医院排名和医院对比直接使用每家医院的一行汇总结果。

## 7. `disease_system_summary`

按 APR MDC 疾病系统编码聚合的疾病系统分布表，用于右侧“疾病系统分布”图表。描述字段不参与分组，仅用于展示编码含义。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `apr_mdc_code` | VARCHAR(30) | APR MDC 疾病系统编码，分组字段。 |
| `apr_mdc_description` | VARCHAR(500) | APR MDC 疾病系统描述，仅用于前端展示。 |
| `discharge_count` | BIGINT | 该疾病系统的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该疾病系统的住院天数总和。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该疾病系统的总收费。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该疾病系统的总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

分组逻辑：`GROUP BY hospital_service_area, hospital_county, facility_name, apr_mdc_code`。

## 8. `diagnosis_summary`

按 CCSR 诊断编码聚合的高发疾病汇总表，用于右侧“高发疾病 Top 10”图表。描述字段不参与分组，仅用于展示编码含义。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `ccsr_diagnosis_code` | VARCHAR(30) | CCSR 具体诊断编码，分组字段。 |
| `ccsr_diagnosis_description` | VARCHAR(500) | CCSR 诊断描述，仅用于前端展示。 |
| `discharge_count` | BIGINT | 该具体诊断的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该具体诊断的住院天数总和。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该具体诊断的总收费。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该具体诊断的总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

分组逻辑：`GROUP BY hospital_service_area, hospital_county, facility_name, ccsr_diagnosis_code`。Top 10 由 Flask 按当前筛选结果的 `discharge_count` 降序获取，不在 Spark 阶段固定截断。

## 9. `patient_risk_summary`

按病情严重程度编码和死亡风险编码聚合的病情风险分布表。严重程度描述不参与分组，仅用于前端展示。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `apr_severity_code` | TINYINT | 病情严重程度编码，分组字段。 |
| `apr_severity_description` | VARCHAR(50) | 病情严重程度描述，仅用于前端展示。 |
| `apr_risk_of_mortality` | TINYINT | 死亡风险编码，分组字段。 |
| `discharge_count` | BIGINT | 该病情风险组合的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该病情风险组合的住院天数总和。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该病情风险组合的总收费。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该病情风险组合的总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

分组逻辑：`GROUP BY hospital_service_area, hospital_county, facility_name, apr_severity_code, apr_risk_of_mortality`。

## 10. `disease_drilldown_summary`

按 APR MDC、APR DRG、CCSR Diagnosis 三层编码聚合的疾病下钻表，用于从疾病系统到病例组再到具体诊断的逐层查看。三个描述字段均不参与分组，仅用于展示编码含义。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | 字符串 | 医院服务区域，全局筛选维度。 |
| `hospital_county` | 字符串 | 医院所在县，全局筛选维度。 |
| `facility_name` | 字符串 | 医疗机构名称，全局筛选维度。 |
| `apr_mdc_code` | VARCHAR(30) | 第 1 层：APR MDC 疾病系统编码，分组字段。 |
| `apr_mdc_description` | VARCHAR(500) | APR MDC 疾病系统描述，仅用于展示。 |
| `apr_drg_code` | VARCHAR(30) | 第 2 层：APR DRG 病例组编码，分组字段。 |
| `apr_drg_description` | VARCHAR(500) | APR DRG 病例组描述，仅用于展示。 |
| `ccsr_diagnosis_code` | VARCHAR(30) | 第 3 层：CCSR 具体诊断编码，分组字段。 |
| `ccsr_diagnosis_description` | VARCHAR(500) | CCSR 诊断描述，仅用于展示。 |
| `discharge_count` | BIGINT | 该完整疾病路径的出院记录数。 |
| `length_of_stay_sum` | DECIMAL/DOUBLE | 该完整疾病路径的住院天数总和。 |
| `total_charges_sum` | DECIMAL/DOUBLE | 该完整疾病路径的总收费。 |
| `total_costs_sum` | DECIMAL/DOUBLE | 该完整疾病路径的总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

分组逻辑：`GROUP BY hospital_service_area, hospital_county, facility_name, apr_mdc_code, apr_drg_code, ccsr_diagnosis_code`。前端下钻顺序为 MDC → DRG → CCSR。

## 表之间的关系

```text
inpatient_discharges
        |
        +-- patient_age_gender_summary
        +-- patient_payment_summary
        +-- patient_disposition_summary
        +-- patient_admission_emergency_summary
        +-- patient_medical_surgical_summary
        +-- hospital_operation_summary
        +-- disease_system_summary
        +-- diagnosis_summary
        +-- patient_risk_summary
        +-- disease_drilldown_summary
```

`inpatient_discharges` 保存明细，九张 `summary` 表保存面向 BI 大屏的预聚合结果。汇总表不是独立业务数据源，重新执行 Spark 任务时会从明细表重新生成。
