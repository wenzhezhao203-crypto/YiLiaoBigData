# 智慧医疗住院数据分析平台需求分析

## 1. 项目背景

医院住院出院数据覆盖医院信息、患者人口统计学特征、疾病分类、手术操作、病情严重程度、费用和支付方式等多个维度。面对百万级记录，人工统计和静态报表难以及时揭示患者结构、疾病负担、医院运营效率和医疗成本之间的关联。

本项目以 Hospital Inpatient Discharge Data 为基础，建设“数据清洗与入库 + Spark 批量聚合 + REST API 服务 + BI 大屏 + AI Agent 辅助交互”的智慧医疗分析平台，为医疗资源优化、疾病负担识别和成本控制提供数据支撑。

## 2. 建设目标

1. 建立统一的数据清洗、标准化和 MySQL 入库流程，保证数据可追溯、可复用。
2. 基于 Spark 完成患者、医院、疾病、费用和支付方式等多维聚合分析。
3. 为前端提供统一、参数化的 Flask RESTful API，避免前端直接查询明细表。
4. 建设三栏式 BI 大屏，直观展示患者结构、医院运营和疾病分析结果。
5. 通过 Agent 接收自然语言问题，调用分析接口并生成基于统计结果的文字洞察。
6. 使用 Docker 统一部署 MySQL、Redis 和 Spark，保证本机开发环境可复现。

## 3. 用户与使用场景

| 用户角色 | 使用场景 | 主要需求 |
|---|---|---|
| 医院管理人员 | 查看运营与成本情况 | 核心 KPI、医院排名、医院对比和资源成本分析。 |
| 医疗资源分析人员 | 比较区域和医院服务负担 | 区域、县、医院筛选；出院量、住院时长、收费和成本分析。 |
| 疾病分析人员 | 了解疾病结构和病情分布 | 疾病系统、高发疾病、严重程度和疾病分层下钻。 |
| 数据分析人员 | 执行数据处理与批量分析 | CSV 清洗、MySQL 入库、Spark 汇总任务和质量核验。 |
| 项目管理员 | 维护项目运行环境 | Docker 组件、数据库、缓存、后端和前端服务管理。 |
| 业务用户 | 获取数据解读 | Agent 问答、工具调用状态和统计结论查看。 |

## 4. 功能需求

### 4.1 数据清洗与入库

| 功能项 | 需求描述 |
|---|---|
| 原始数据读取 | 支持批量读取住院出院 CSV 数据，保留源文件名和源行号。 |
| 字段标准化 | 将原始列名映射为统一的 snake_case 字段名。 |
| 类型转换 | 对住院天数、出生体重、总收费、总成本等字段执行数值转换和校验。 |
| 缺失与异常处理 | 按字段业务规则处理缺失、重复、非法枚举值和异常数值。 |
| 编码保留 | 医院证书号、机构 ID、APR 编码和邮编按字符串保存，避免前导零丢失。 |
| 增量去重 | 使用清洗后业务字段的 SHA-256 指纹作为 `source_row_hash`，避免重复入库。 |
| 敏感数据处理 | 处理脱敏机构和无法用于医院分析的敏感记录，避免在大屏中展示无效机构信息。 |
| 明细入库 | 将清洗结果写入 `inpatient_discharges`，供 Spark 分析和后续追溯查询使用。 |

### 4.2 Spark 多维分析

Spark 从 `inpatient_discharges` 读取清洗后的住院明细，在单机 `local[*]` 模式下完成聚合，并将结果写回 MySQL。所有汇总表保留医院服务区域、医院所在县和医疗机构名称三个筛选字段。

| 分析模块 | 分组维度 | 核心指标 | 业务价值 |
|---|---|---|---|
| 年龄与性别 | `age_group`、`gender` | 出院量、年龄组占比、性别结构 | 识别患者人口结构。 |
| 支付方式 | `payment_typology_1` | 出院量、占比、总收费、总成本、平均收费、平均成本 | 分析医保、商业保险和其他支付构成。 |
| 离院去向 | `patient_disposition` | 出院量、占比、平均住院天数、收费和成本 | 识别居家、护理、转院和临终关怀等结果。 |
| 入院与急诊 | `type_of_admission`、急诊标记 | 出院量、入院结构、急诊结构、平均住院天数 | 观察急诊、择期、创伤和新生儿等入院构成。 |
| 内科与外科 | `apr_medical_surgical_description` | 出院量、占比、平均住院天数、平均收费、平均成本 | 对比 Medical 与 Surgical 病例结构。 |
| 医院运营 | 区域、县、医院 | 出院量、住院天数、收费、成本、急诊占比 | 支持医院排名、比较和资源成本分析。 |
| 疾病系统 | `apr_mdc_code` | 出院量、占比、住院天数、收费和成本 | 展示疾病系统整体负担。 |
| 高发疾病 | `ccsr_diagnosis_code` | 出院量、占比、住院天数、收费和成本 | 识别住院量最高的具体诊断。 |
| 病情严重程度 | `apr_severity_code` | 出院量、占比、收费和成本 | 观察 Minor、Moderate、Major、Extreme 的构成。 |
| 疾病分层 | MDC → DRG → CCSR | 出院量、收费、成本、平均住院天数 | 支持疾病层级下钻。 |

### 4.3 BI 大屏展示

BI 大屏采用顶部全局筛选、左中右三栏布局。全局筛选仅保留以下条件，并按“区域 → 县 → 医院”进行级联联动：

- `hospital_service_area`：医院服务区域。
- `hospital_county`：医院所在县。
- `facility_name`：医疗机构名称。

| 展示区域 | 图表或模块 | 展示内容 |
|---|---|---|
| 左侧患者分析 | 年龄与性别结构 | 按年龄组顺序展示男女堆叠柱状图。 |
| 左侧患者分析 | 支付方式分布 | 环形图展示支付方式占比；支付类型归并为 Medicare、Medicaid、Blue Cross/Blue Shield、Private Health Insurance 和 Other。 |
| 左侧患者分析 | 离院去向 Top 10 | 按出院量降序展示横向条形图。 |
| 左侧患者分析 | 入院与急诊结构 | 默认显示入院类型与急诊标记堆叠柱状图，可切换为内科与外科结构图。 |
| 中部医院运营 | 核心运营指标 | 展示医院数、出院量、平均住院天数、总收费、总成本、平均收费、平均成本和急诊占比。 |
| 中部医院运营 | 医院资源与成本 | 以出院量为横轴、平均成本为纵轴的医院气泡图，支持悬停详情和弹窗放大。 |
| 中部医院运营 | 医院运营排名 | 支持按出院量、总收费、平均住院天数和平均成本排序。 |
| 中部医院运营 | 医院对比表 | 支持分页、医院名称模糊搜索和指标比较。 |
| 右侧疾病分析 | 疾病系统分布 | 展示 APR MDC 编码、出院量和疾病系统描述。 |
| 右侧疾病分析 | 高发疾病 Top 10 | 展示 CCSR 诊断名称和住院量。 |
| 右侧疾病分析 | 病情严重程度 | 展示四级病情严重程度的出院量分布。 |
| 右侧疾病分析 | 疾病分层下钻 | 使用弹窗气泡图从 MDC 进入 DRG，再进入 CCSR。 |

### 4.4 后端 API 服务

后端 API 统一使用 `/api` 前缀和 JSON 响应格式。除筛选候选项接口外，所有大屏数据接口支持区域、县和医院三个可选筛选参数。

| 接口类别 | 接口范围 | 用途 |
|---|---|---|
| 全局筛选 | `/api/filters/areas`、`/counties`、`/facilities` | 返回级联下拉候选项。 |
| 患者分析 | `/api/dashboard/patient/*` | 返回年龄性别、支付、离院、入院急诊和内外科数据。 |
| 医院运营 | `/api/dashboard/kpi`、`/api/dashboard/hospital/*` | 返回 KPI、资源、排名和医院对比数据。 |
| 疾病分析 | `/api/dashboard/disease/*` | 返回疾病系统、高发疾病、严重程度和下钻数据。 |
| Agent 服务 | Agent 入口接口 | 接收自然语言问题，返回文本回复和工具调用状态。 |

接口必须使用参数化 SQL，禁止直接拼接用户输入；对分页、排序、limit 和模糊搜索参数进行校验；没有数据时返回 HTTP 200 和空数据结构。

### 4.5 AI Agent 辅助交互

Agent 是 BI 大屏的辅助交互入口，不替代可视化大屏。功能要求如下：

1. 接收医院运营、患者结构、急诊、疾病和费用相关的自然语言问题。
2. 识别问题意图和区域、县、医院等筛选条件。
3. 使用 LangChain 工具调用 Flask 分析接口。
4. 基于接口返回的结构化数据，由大模型生成通俗、可追溯的文字解读。
5. 阶段一响应至少返回 `reply` 和 `tool_calls`，供前端展示回答和调用状态。
6. 超出住院数据分析范围的问题必须拒答，不得编造数据、诊断或治疗建议。

## 5. 数据需求

### 5.1 明细数据

`inpatient_discharges` 为清洗后的住院出院明细主表，每行对应一条出院记录，保存医院信息、患者特征、疾病编码、支付方式、住院时长、收费和成本。`source_row_hash` 支持增量去重，`source_file_name` 和 `source_row_number` 支持源数据追溯。

### 5.2 预聚合数据

当前 Spark 汇总表包括：

```text
patient_age_gender_summary
patient_payment_summary
patient_disposition_summary
patient_admission_emergency_summary
patient_medical_surgical_summary
hospital_operation_summary
disease_system_summary
diagnosis_summary
patient_risk_summary
disease_drilldown_summary
```

汇总表仅服务于分析展示，不替代明细表。平均住院天数、平均收费和平均成本必须由聚合总量除以出院记录数计算，不能对医院平均值再次简单求平均。

## 6. 非功能需求

### 6.1 性能

- BI 大屏优先查询 Spark 预聚合表，避免直接扫描百万级明细数据。
- Redis 用于高频接口的缓存扩展，缓存键应包含完整筛选条件和接口参数。
- Spark 批任务可重复运行并覆盖重建汇总结果。
- 前端图表应适配不同屏幕，tooltip、长文本和表格不得造成页面横向溢出。

### 6.2 安全与隐私

- 数据库密码和 LLM API Key 仅存放在本地 `.env`，不得提交到 Git 仓库。
- `.env.example` 仅提供配置模板和示例值，不保存真实密钥。
- API 使用参数化 SQL，并校验全部输入参数。
- 脱敏医院或缺少医院定位信息的无效记录不进入医院分析展示。
- Agent 不得输出个体身份信息，也不得提供诊断、处方或医疗决策建议。

### 6.3 可维护性与兼容性

- 数据清洗规则、表结构、接口、前端展示和部署方式均应独立文档化。
- Spark 任务、SQL 初始化脚本、Flask 路由和 Next.js 组件按模块组织。
- 前端支持 Chrome、Edge 和 Firefox 等现代浏览器，适配 1920×1080、1440×900、1280×800 及较窄视口。
- 后端可在 Windows Python 环境运行；MySQL、Redis、Spark 通过 Docker Compose 运行。

## 7. 技术与部署要求

| 系统层 | 技术方案 |
|---|---|
| 数据清洗 | Python、Pandas、SQLAlchemy、MySQL Connector/Python |
| 数据存储 | MySQL 8.0，保存住院明细和分析汇总表 |
| 缓存 | Redis 7 |
| 大数据计算 | Apache Spark 4.0.x，单机 `local[*]` 模式 |
| 后端 API | Flask、SQLAlchemy、Flask-CORS |
| AI Agent | Python、LangChain、LLM API |
| 前端 | Next.js、TypeScript、ECharts、Lucide Icons |
| 容器化 | Docker Compose |
| 版本管理 | Git、GitHub |

当前版本不要求 Hadoop HDFS、Hive 或 Spark Master/Worker 集群演示；后续可根据数据量和部署场景扩展为集群架构。

## 8. 项目边界与后续扩展

当前版本重点完成描述性统计和多维聚合分析，不包含个体患者身份识别、临床诊断、治疗推荐、处方建议、实时医院系统采集、时间序列预测模型和 Hadoop 集群部署。

后续可扩展方向包括增量同步、数据质量监控、费用异常检测、疾病趋势预测、医院地图、权限管理、审计日志和分析报告导出。

## 9. 验收标准

1. 可按照清洗规则读取 CSV，并将清洗数据写入 `inpatient_discharges`。
2. Spark 可从 MySQL 读取明细并生成全部分析汇总表。
3. Flask API 可在三个全局筛选条件下返回正确 JSON，且可处理空数据和非法参数。
4. BI 大屏可展示患者、医院运营和疾病分析三栏内容，并支持筛选联动。
5. 医院对比表支持分页、排序和医院名称模糊搜索；疾病下钻支持 MDC、DRG、CCSR 三级浏览。
6. Agent 可调用分析接口并返回文字回复和工具调用状态；超出范围的问题可拒答。
7. `.env` 未被 Git 跟踪，`.env.example` 可用于新环境初始化。
8. 项目可按照文档启动 Docker 组件、Flask 服务和 Next.js 前端。
