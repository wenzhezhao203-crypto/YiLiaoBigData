# day1
**当前项目方案**

项目采用精简架构，不部署 Hadoop、HDFS、Hive。MySQL 承担结构化数据存储与查询，Spark 负责单机批量清洗和分析，Redis 用于缓存；Flask 提供数据 API，FastAPI + LangChain 实现 AI Agent，Vue + ECharts 负责展示。

```text
原始 CSV/TSV
  -> 数据清洗
  -> MySQL 明细数据
  -> Spark 单机分析
  -> MySQL 分析结果
  -> Flask API + Redis 缓存
  -> FastAPI / LangChain Agent
  -> Vue + ECharts 数据大屏与问答页面
```

**五人分工**

| 成员 | 核心职责 | 主要交付 |
|---|---|---|
| 成员 1 | 数据清洗与 MySQL 入库 | Pandas 清洗脚本、MySQL 建表 SQL、清洗后明细表、数据质量统计与导入说明 |
| 成员 2 | Spark 数据分析 | 从 MySQL 读取数据，完成疾病、年龄、医院、年份、费用、支付方式等聚合分析，并写回 MySQL 分析结果表 |
| 成员 3 | Flask 后端接口 | 查询明细与分析结果的 RESTful API、参数校验、统一 JSON 响应、Redis 缓存、接口文档 |
| 成员 4 | AI Agent | FastAPI 服务、LangChain 意图识别、调用 Flask API、文字洞察、ECharts 图表配置、多轮对话 |
| 成员 5 | Docker 与 Git 管理 | `docker-compose.yml`、MySQL/Redis/Spark 组件版本管理、数据卷与端口配置、Git 分支和提交规范、部署文档 |

接口边界已确定：前端和 Agent 只能调用 Flask API；Agent 不直接访问 MySQL；Spark 不直接给前端提供接口。

**开发工具与依赖**

| 类别 | 工具与版本 | 用途 |
|---|---|---|
| Python | `Python 3.11.x` | 全组统一开发语言 |
| 数据清洗 | `pandas 2.2.x`、`numpy`、`PyMySQL`、`SQLAlchemy` | 清洗、类型转换与 MySQL 入库 |
| Spark | `Spark 4.0.1`、`pyspark 4.0.1` | 单机大数据分析，版本必须一致 |
| 后端 | `Flask 3.0.x`、`flask-cors`、`redis` | 数据查询 API 与缓存 |
| AI 服务 | `FastAPI 0.115.x`、`uvicorn`、`LangChain 0.3.x`、`httpx` | Agent、自然语言分析与工具调用 |
| 前端 | `Node.js 20 LTS`、`Vue 3`、`ECharts 5` | 数据看板、聊天界面和图表 |
| 开发与管理 | Docker Desktop、Git、VS Code/PyCharm、Postman/Apifox、DBeaver/Navicat | 容器、版本控制、接口测试和数据库管理 |

**Docker 组件**

已生成部署文件：[docker-compose.yml](C:\Users\86180\Desktop\医疗项目src\docker-compose.yml)。

| 容器 | 镜像 | 端口 | 用途 |
|---|---|---:|---|
| MySQL | `mysql:8.0` | `3306` | 医疗明细数据、分析结果、任务记录 |
| Redis | `redis:7-alpine` | `6379` | API 热点缓存、Agent 会话缓存 |
| Spark | `apache/spark:4.0.1` | `4040` | 单机模式 `local[*]` 数据处理与分析 |

当前 MySQL 配置：

```text
数据库：medical_platform
root 密码：123456
业务账号：medical_app
业务账号密码：123456
```

Spark 使用单容器模式，不需要 `spark-master` 和 `spark-worker`：

```bash
spark-submit --master local[*] /app/spark_jobs/analysis.py
```

**建议的数据分析展示方向**

- 医疗服务总览：出院量、医院数、总费用、总成本、平均住院天数、急诊比例。
- 疾病负担：高发疾病、疾病大类分布、疾病年度变化。
- 费用与成本：疾病/医院/人群的平均费用、平均成本、费用成本差额。
- 住院效率：住院时长、长住院病例、不同医院或疾病的资源占用。
- 风险分析：病情严重程度、死亡风险与费用、住院时长的关系。
- 医院运营：医院服务量、急诊比例、病例结构、费用和住院效率对比。
- 人群画像：年龄、性别、种族、民族、ZIP3 对疾病和费用的影响。
- 医保支付：支付方式占比及其对应的疾病、费用、住院天数。
- 诊断与操作：疾病、操作、内外科分类之间的关联。
- 离院去向：居家、转院、居家护理、自动离院等分布。

数据是住院出院摘要，不应将其用于严格的患者再入院率计算；所有敏感人群字段应以聚合形式展示。
***