# 智慧医疗住院数据分析平台后端接口结构

本文档定义 BI 大屏 Flask RESTful API 的接口边界。接口优先查询 Spark 已生成的九张汇总表；只有后续增加住院明细追溯功能时才访问 `inpatient_discharges`。

Base URL：`/api`

## 1. 通用约定

### 1.1 全局筛选参数

除筛选候选项接口外，所有大屏数据接口均支持以下可选查询参数：

| 参数 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | string | 医院服务区域。 |
| `hospital_county` | string | 医院所在县。 |
| `facility_name` | string | 医疗机构名称。 |

未传递参数表示不限制该维度。后端使用参数化 SQL 构建查询，不能直接拼接用户输入。

### 1.2 通用成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": [],
  "filters": {
    "hospital_service_area": null,
    "hospital_county": null,
    "facility_name": null
  },
  "updated_at": "2026-08-15T16:30:00+08:00"
}
```

`updated_at` 取当前查询汇总表中的最新 `updated_at`。没有数据时返回空数组或空对象，HTTP 状态码仍为 `200`。

### 1.3 通用失败响应

```json
{
  "code": 40001,
  "message": "invalid query parameter",
  "data": null
}
```

建议状态码：参数错误使用 `400`，未授权使用 `401/403`，服务异常使用 `500`。

## 2. 全局筛选接口

### 2.1 获取服务区域

```text
GET /api/filters/areas
```

数据来源：`hospital_operation_summary`。

返回数据：

```json
{
  "data": [
    { "hospital_service_area": "New York City" },
    { "hospital_service_area": "Long Island" }
  ]
}
```

前端用途：初始化“医院服务区域”下拉框。

### 2.2 获取县列表

```text
GET /api/filters/counties?hospital_service_area=New%20York%20City
```

请求参数：`hospital_service_area` 可选；传入时仅返回该区域下的县。

返回数据：

```json
{
  "data": [
    { "hospital_county": "Bronx" },
    { "hospital_county": "Kings" }
  ]
}
```

前端用途：服务区域变化后刷新“医院所在县”下拉框。

### 2.3 获取医院列表

```text
GET /api/filters/facilities?hospital_service_area=New%20York%20City&hospital_county=Bronx
```

请求参数：`hospital_service_area`、`hospital_county` 均可选。

返回数据：

```json
{
  "data": [
    { "facility_name": "Montefiore Medical Center - Henry & Lucy Moses Div" }
  ]
}
```

前端用途：区域或县变化后刷新“医疗机构名称”下拉框。

## 3. 患者分析接口

### 3.1 年龄与性别结构

```text
GET /api/dashboard/patient/age-gender
```

数据来源：`patient_age_gender_summary`。

返回每个年龄组和性别组合的数据：

```json
{
  "data": [
    {
      "age_group": "50 to 69",
      "age_group_sort": 4,
      "gender": "F",
      "discharge_count": 235401,
      "discharge_ratio": 0.1126
    }
  ]
}
```

前端用途：按 `age_group_sort` 排序绘制年龄与性别堆叠柱状图；`discharge_ratio` 用于提示框展示占比。

### 3.2 支付方式分布

```text
GET /api/dashboard/patient/payment
```

数据来源：`patient_payment_summary`。

返回每种主要支付方式的数据：

```json
{
  "data": [
    {
      "payment_typology_1": "Medicare",
      "discharge_count": 812345,
      "discharge_ratio": 0.3904,
      "total_charges": 52345678901.23,
      "total_costs": 15678901234.56,
      "average_charge": 64434.21,
      "average_cost": 19299.11
    }
  ]
}
```

前端用途：支付方式环形图、支付金额提示框。

### 3.3 离院去向 Top 10

```text
GET /api/dashboard/patient/disposition?limit=10
```

数据来源：`patient_disposition_summary`。

请求参数：`limit` 可选，默认 `10`，范围 `1-20`。

返回数据：

```json
{
  "data": [
    {
      "patient_disposition": "Home or Self Care",
      "discharge_count": 1366724,
      "discharge_ratio": 0.6562,
      "average_length_of_stay": 4.78,
      "total_charges": 70050796938.21,
      "total_costs": 21374296075.13,
      "average_charge": 51255.42,
      "average_cost": 15638.89
    }
  ]
}
```

前端用途：按 `discharge_count` 降序的离院去向横向条形图。

### 3.4 入院与急诊结构

```text
GET /api/dashboard/patient/admission-emergency
```

数据来源：`patient_admission_emergency_summary`。

返回数据：

```json
{
  "data": [
    {
      "type_of_admission": "Emergency",
      "emergency_department_indicator": "Y",
      "discharge_count": 658123,
      "discharge_ratio": 0.3163,
      "average_length_of_stay": 5.21
    }
  ]
}
```

前端用途：按入院类型和急诊标记绘制堆叠柱状图。

## 4. 医院运营接口

### 4.1 核心运营指标

```text
GET /api/dashboard/kpi
```

数据来源：`hospital_operation_summary`。

返回数据：

```json
{
  "data": {
    "hospital_count": 56,
    "discharge_count": 2080596,
    "average_length_of_stay": 5.34,
    "total_charges": 150584460749.98,
    "total_costs": 45111459703.41,
    "average_charge": 72373.58,
    "average_cost": 21682.02,
    "emergency_ratio": 0.4922,
    "emergency_charges": 96840319473.04,
    "non_emergency_charges": 53744141276.94,
    "emergency_costs": 28785196049.97,
    "non_emergency_costs": 16326263653.44
  }
}
```

前端用途：中部 KPI 卡片和急诊/非急诊收费成本对比。

### 4.2 医院资源与成本

```text
GET /api/dashboard/hospital/resources
```

数据来源：`hospital_operation_summary`。

返回每家医院的气泡图数据：

```json
{
  "data": [
    {
      "facility_name": "Bellevue Hospital Center",
      "hospital_county": "Manhattan",
      "discharge_count": 7652,
      "average_length_of_stay": 5.21,
      "average_cost": 10325.45,
      "emergency_ratio": 0.5235
    }
  ]
}
```

前端用途：医院资源与成本气泡图，横轴为出院量，纵轴为平均成本。

### 4.3 医院运营排名

```text
GET /api/dashboard/hospital/ranking?sort_by=discharge_count&order=desc&limit=10
```

数据来源：`hospital_operation_summary`。

请求参数：

| 参数 | 可选值 | 默认值 |
|---|---|---|
| `sort_by` | `discharge_count`、`total_charges`、`average_length_of_stay`、`average_cost` | `discharge_count` |
| `order` | `asc`、`desc` | `desc` |
| `limit` | `1-50` | `10` |

返回数据：

```json
{
  "data": [
    {
      "rank": 1,
      "facility_name": "Bellevue Hospital Center",
      "hospital_county": "Manhattan",
      "discharge_count": 7652,
      "total_charges": 158245000.00,
      "average_length_of_stay": 5.21,
      "average_cost": 10325.45
    }
  ]
}
```

前端用途：中部医院运营排名条形图。

### 4.4 医院对比表

```text
GET /api/dashboard/hospital/comparison?page=1&page_size=20&sort_by=discharge_count&order=desc
```

数据来源：`hospital_operation_summary`。

请求参数：`page` 默认 `1`；`page_size` 默认 `20`、最大 `100`；`sort_by` 和 `order` 规则与医院排名接口一致；可选 `keyword` 用于医院名称模糊搜索。

返回数据：

```json
{
  "data": {
    "items": [
      {
        "facility_name": "Bellevue Hospital Center",
        "hospital_county": "Manhattan",
        "discharge_count": 7652,
        "average_length_of_stay": 5.21,
        "total_charges": 158245000.00,
        "total_costs": 78965000.00,
        "average_cost": 10325.45,
        "emergency_ratio": 0.5235
      }
    ],
    "page": 1,
    "page_size": 20,
    "total": 56
  }
}
```

前端用途：中部医院对比明细表、分页和排序。

## 5. 疾病与风险接口

### 5.1 疾病系统分布

```text
GET /api/dashboard/disease/systems
```

数据来源：`disease_system_summary`。

返回数据：

```json
{
  "data": [
    {
      "apr_mdc_code": "04",
      "apr_mdc_description": "DISEASES AND DISORDERS OF THE RESPIRATORY SYSTEM",
      "discharge_count": 211390,
      "discharge_ratio": 0.1016,
      "average_length_of_stay": 5.37,
      "total_charges": 15436789123.45,
      "total_costs": 4567890123.45
    }
  ]
}
```

前端用途：右侧疾病系统柱状图或环形图。

### 5.2 高发疾病 Top 10

```text
GET /api/dashboard/disease/top-diagnoses?limit=10
```

数据来源：`diagnosis_summary`。

请求参数：`limit` 默认 `10`，范围 `1-30`。

返回数据：

```json
{
  "data": [
    {
      "ccsr_diagnosis_code": "RSP009",
      "ccsr_diagnosis_description": "ASTHMA",
      "discharge_count": 4562,
      "discharge_ratio": 0.0022,
      "average_length_of_stay": 4.32,
      "total_charges": 285678901.23,
      "total_costs": 85678901.23
    }
  ]
}
```

前端用途：右侧高发疾病 Top 10 横向条形图。

### 5.3 病情严重程度分布

```text
GET /api/dashboard/disease/risk
```

数据来源：`patient_risk_summary`。

返回数据（死亡风险字段当前不参与展示）：

```json
{
  "data": [
    {
      "apr_severity_code": 3,
      "apr_severity_description": "Major",
      "discharge_count": 14256,
      "discharge_ratio": 0.0069,
      "average_length_of_stay": 6.28,
      "total_charges": 986543210.12,
      "total_costs": 286543210.12
    }
  ]
}
```

前端用途：右侧病情严重程度柱状图，展示 Minor、Moderate、Major、Extreme 四类病情严重程度的出院量和占比。接口只统计 `apr_severity_code` 为 1-4 的有效记录，严重程度为空的记录不进入图表。

### 5.4 疾病分层下钻

```text
GET /api/dashboard/disease/drilldown?level=mdc
GET /api/dashboard/disease/drilldown?level=drg&apr_mdc_code=04
GET /api/dashboard/disease/drilldown?level=ccsr&apr_mdc_code=04&apr_drg_code=137
```

数据来源：`disease_drilldown_summary`。

请求参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| `level` | string | 必填，取值 `mdc`、`drg`、`ccsr`。 |
| `apr_mdc_code` | string | 当 `level=drg` 或 `level=ccsr` 时必填。 |
| `apr_drg_code` | string | 当 `level=ccsr` 时必填。 |

返回 MDC 层数据：

```json
{
  "data": [
    {
      "code": "04",
      "description": "DISEASES AND DISORDERS OF THE RESPIRATORY SYSTEM",
      "discharge_count": 211390,
      "total_charges": 15436789123.45,
      "total_costs": 4567890123.45,
      "average_length_of_stay": 5.37
    }
  ],
  "breadcrumb": []
}
```

返回 DRG 或 CCSR 层时字段保持 `code`、`description`、`discharge_count`、`total_charges`、`total_costs`、`average_length_of_stay` 一致，并返回当前 MDC/DRG 的 `breadcrumb`。

前端用途：疾病系统 → 病例组 → 具体诊断的三级下钻卡片和面包屑导航。

## 6. 接口与数据库表关系

```text
筛选接口                  → hospital_operation_summary
患者分析接口              → patient_age_gender_summary
                          → patient_payment_summary
                          → patient_disposition_summary
                          → patient_admission_emergency_summary
医院运营接口              → hospital_operation_summary
疾病与风险接口            → disease_system_summary
                          → diagnosis_summary
                          → patient_risk_summary
疾病分层下钻接口          → disease_drilldown_summary
住院明细追溯（后续扩展）  → inpatient_discharges
```
