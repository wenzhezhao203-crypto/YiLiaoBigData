这是一个以医院住院出院数据为核心的“数据治理 + 多维分析 + AI自然语言交互 + 可视化决策支持”平台，而不是单纯的聊天机器人或数据看板。

项目主链路是：

`原始 CSV/TSV 数据 -> 清洗与标准化 -> MySQL/HDFS 持久化 -> Spark/Hive 分析计算 -> Flask REST API -> AI Agent 意图识别与工具调用 -> Vue/ECharts 展示与报告`

系统由三个核心模块组成：

1. 数据预处理与持久化  
   用 Pandas 对百万级住院数据批量读取、去重、缺失/异常处理、字段类型统一，并将清洗结果入 MySQL；后续可扩展 HDFS、备份恢复、增量同步及数据质量评分。

2. 大数据分析服务  
   用 Spark/Hive 完成疾病、年龄、医院、年份、支付方式、费用、住院时长、严重程度等维度的聚合与统计；通过 Flask 以统一 JSON API 输出结果、指标说明和查询耗时。Redis 用于高频查询缓存。

3. AI 智能交互  
   用户用自然语言提问，例如“2023 年不同年龄段心血管疾病的平均住院费用是多少？”；LangChain/AI Agent 将问题解析为分析参数，调用后端 API，再生成文字洞察、ECharts 图表配置和医疗分析报告。

从架构图看，分层关系也很明确：

- 数据源层：原始住院数据、MySQL、Redis、HDFS
- 数据处理层：读取、清洗、标准化、结构化入库
- 服务层：RESTful API、算法封装、数据分析、AI 调用
- AI 智能层：LangChain、Agent、工具/API 编排
- 展示层：Web 交互、可视化大屏、医疗洞察报告

一期应优先实现可闭环、可演示的核心能力：数据清洗入库、若干分析 API、自然语言问题到 API 的映射、文字摘要与 ECharts 图表。关联规则、费用预测、再入院风险、3D 地图、本地 LLM、全量 Hadoop 集群等适合放在二期。
