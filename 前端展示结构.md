# 智慧医疗住院数据分析大屏展示结构

全局筛选只保留：
```
医院服务区域｜医院所在县｜医疗机构名称
```
三者应级联联动：
```
服务区域 -> 县 -> 医院(从高到低)
```
| 区域 | 图表 | 字段与指标 | 展示目的 |
|---|---|---|---|
| 左侧 | 年龄与性别结构 | `age_group`、`age_group_sort`、`gender`、出院记录数 | 了解患者年龄层和性别结构 |
| 左侧 | 支付方式分布 | `payment_typology_1`、出院记录数、总收费 | 展示医保、商业保险、自费等支付结构 |
| 左侧 | 离院去向 Top N | `patient_disposition`、出院记录数 | 展示居家、护理院、转院、临终关怀、自行离院等结果 |
| 左侧 | 入院与急诊结构 | `type_of_admission`、`emergency_department_indicator`、出院记录数 | 观察急诊、择期、紧急和新生儿入院构成 |
| 中部 | 核心运营指标 | 医院数、出院量、平均住院天数、总收费、总成本、平均收费、急诊占比 | 快速概览当前筛选范围内的医院运营情况 |
| 中部 | 医院资源与成本 | `facility_name`、平均住院天数、平均成本、出院量、急诊占比 | 定位高服务量、高成本、长住院医院 |
| 中部 | 医院运营排名 | `facility_name`、出院量、总收费、平均住院天数、平均成本 | 支持按不同指标对医院排序比较 |
| 中部 | 医院对比表 | 医院、县、出院量、平均住院天数、总收费、总成本、急诊占比 | 供用户查看具体医院数据 |
| 右侧 | 疾病系统分布 | `apr_mdc_code`、`apr_mdc_description`、出院记录数 | 展示 26 类疾病系统的整体负担 |
| 右侧 | 高发疾病 Top 10 | `ccsr_diagnosis_code`、`ccsr_diagnosis_description`、出院记录数 | 识别住院量最高的具体疾病 |
| 右侧 | 病情严重程度分布 | `apr_severity_code`、`apr_severity_description`、出院记录数 | 展示 Minor、Moderate、Major、Extreme 四类病情严重程度构成 |
| 右侧 | 疾病分层下钻图 | `apr_mdc_code` → `apr_drg_code` → `ccsr_diagnosis_code`、出院记录数 | 从疾病系统逐层查看病例组和具体诊断 |


┌──────────────────────────────────────────────────────────────────────┐
│ 智慧医疗住院数据分析大屏     服务区域｜医院所在县｜医疗机构名称         │
├────────────────┬──────────────────────────────────┬────────────────┤
│ 左侧：患者画像  │ 中部：医院资源与成本              │ 右侧：疾病与风险 │
│                │                                  │                │
│ 年龄与性别结构  │ 核心运营指标                      │ 疾病系统分布     │
│ 支付方式分布    │ 医院资源与成本气泡图              │ 高发疾病 Top 10  │
│ 离院去向 Top N  │ 医院运营排名                      │ 病情严重程度分布 │
│ 入院与急诊结构  │ 医院对比明细表                    │                │
└────────────────┴──────────────────────────────────┴────────────────┘

# 数据表映射与交互规则

当前共有 1 张住院明细表和 9 张 Spark 预聚合表。大屏所有图表优先读取对应的汇总表；仅在后续需要追溯单条住院记录时读取 `inpatient_discharges`。

脱敏医院记录已在清洗阶段删除，因此不在前端展示，也不提供脱敏医院筛选项。

## 全局筛选规则

所有图表接口统一接收以下可选参数：

| 参数 | 来源字段 | 规则 |
|---|---|---|
| 服务区域 | `hospital_service_area` | 第一级筛选，未选择时查询全部区域。 |
| 医院所在县 | `hospital_county` | 第二级筛选，候选项受已选区域限制。 |
| 医疗机构名称 | `facility_name` | 第三级筛选，候选项受已选区域和县限制。 |

筛选器级联关系为“区域 → 县 → 医院”。变更上级筛选值时清空下级筛选值；筛选改变后左、中、右区域并行刷新。无数据时显示“暂无数据”，平均值的除数为 0 时显示 `--`。

## 左侧：患者分析

| 图表 | 数据表 | 字段 | 展示和计算规则 |
|---|---|---|---|
| 年龄与性别结构 | `patient_age_gender_summary` | `age_group`、`age_group_sort`、`gender`、`discharge_count` | 按 `age_group_sort` 升序的分组或堆叠柱状图；占比 = 分类出院量 / 当前筛选总出院量。 |
| 支付方式分布 | `patient_payment_summary` | `payment_typology_1`、`discharge_count`、`total_charges_sum`、`total_costs_sum` | 环形图展示出院量占比；提示框显示平均收费和平均成本。 |
| 离院去向 Top 10 | `patient_disposition_summary` | `patient_disposition`、`discharge_count`、`length_of_stay_sum`、`total_charges_sum`、`total_costs_sum` | 按出院量降序取前 10 的横向条形图；提示框显示平均住院天数、平均收费、平均成本。 |
| 入院与急诊结构 | `patient_admission_emergency_summary` | `type_of_admission`、`emergency_department_indicator`、`discharge_count`、`length_of_stay_sum` | 按入院类型的堆叠柱状图，区分 `Y`、`N` 和空急诊标记。 |

左侧通用计算：平均住院天数 = `length_of_stay_sum / discharge_count`；平均收费 = `total_charges_sum / discharge_count`；平均成本 = `total_costs_sum / discharge_count`。

## 中部：医院运营

中部四个模块均使用 `hospital_operation_summary`。该表按 `hospital_service_area`、`hospital_county`、`facility_name` 聚合，每行代表一个医院。

### 核心运营指标

| 指标 | 计算规则 |
|---|---|
| 医院数 | `COUNT(DISTINCT facility_name)` |
| 出院量 | `SUM(discharge_count)` |
| 平均住院天数 | `SUM(length_of_stay_sum) / SUM(discharge_count)` |
| 总收费、总成本 | 分别为 `SUM(total_charges_sum)`、`SUM(total_costs_sum)` |
| 平均收费、平均成本 | 总收费或总成本除以总出院量 |
| 急诊占比 | `SUM(emergency_yes_count) / SUM(emergency_known_count)` |
| 急诊/非急诊收费与成本 | 分别汇总 `emergency_charges_sum`、`non_emergency_charges_sum`、`emergency_costs_sum`、`non_emergency_costs_sum`。 |

### 医院资源与成本

使用气泡图或散点图：X 轴为 `discharge_count`，Y 轴为平均成本 `total_costs_sum / discharge_count`，气泡大小为出院量，颜色为平均住院天数或急诊占比。提示框显示医院、县、出院量、平均住院天数、平均成本和急诊占比。

### 医院运营排名与对比表

排名支持按出院量、总收费、平均住院天数、平均成本排序，默认显示 Top 10。对比表展示 `facility_name`、`hospital_county`、出院量、平均住院天数、总收费、总成本、平均成本和急诊占比，并支持分页、排序与医院名称搜索。

平均指标应由 Flask 基于总和和记录数计算，不能对医院平均值再次求平均。

## 右侧：疾病与风险

| 图表 | 数据表 | 编码分组字段 | 描述展示字段 | 展示规则 |
|---|---|---|---|---|
| 疾病系统分布 | `disease_system_summary` | `apr_mdc_code` | `apr_mdc_description` | 柱状图或环形图展示 `discharge_count` 与占比；提示框可显示总收费、总成本和平均住院天数。 |
| 高发疾病 Top 10 | `diagnosis_summary` | `ccsr_diagnosis_code` | `ccsr_diagnosis_description` | 当前筛选结果按 `discharge_count` 降序取前 10；使用横向条形图。 |
| 病情严重程度分布 | `patient_risk_summary` | `apr_severity_code` | `apr_severity_description` | 柱状图展示 Minor、Moderate、Major、Extreme 四类病情严重程度的出院量。 |
| 疾病分层下钻图 | `disease_drilldown_summary` | `apr_mdc_code`、`apr_drg_code`、`ccsr_diagnosis_code` | 三个对应的 description 字段 | 以出院量展示 MDC → DRG → CCSR 的三级疾病路径。 |

疾病描述字段只用于显示，Spark 分组的唯一依据是编码。建议前端统一显示为“编码 - 描述”。

疾病下钻流程：

1. 初始层展示 MDC 疾病系统。
2. 用户点击 MDC 后，携带 `apr_mdc_code` 展示该系统下的 DRG 病例组。
3. 用户点击 DRG 后，继续携带 `apr_drg_code` 展示 CCSR 具体诊断。
4. 面包屑显示“全部疾病系统 → MDC → DRG → CCSR”，每次下钻始终保留区域、县、医院三个全局筛选条件。

每层可展示 `discharge_count`，提示框显示 `total_charges_sum`、`total_costs_sum` 和平均住院天数。

## 数据来源关系

```text
inpatient_discharges
        ├── patient_age_gender_summary
        ├── patient_payment_summary
        ├── patient_disposition_summary
        ├── patient_admission_emergency_summary
        ├── hospital_operation_summary
        ├── disease_system_summary
        ├── diagnosis_summary
        ├── patient_risk_summary
        └── disease_drilldown_summary
```
