# 智慧医疗住院数据分析平台数据存储说明文档

## 1. 引言

### 1.1 编写目的

本文档依据智慧医疗住院数据分析平台的实际实现编写，说明原始数据、清洗后明细数据、Spark 汇总结果和缓存数据的存储方案、部署方式、数据结构与安全约束。

本文档作为数据清洗、Spark 批处理、Flask 接口、BI 大屏和智能问答模块开发的共同依据，供开发人员、测试人员、数据分析人员和运维人员参考。文档只描述当前已落地的能力；列式数据库和分布式文件系统部分同时记录后续可扩展方向。

### 1.2 背景

智慧医疗住院数据分析平台以 SPARCS 去标识化住院出院 CSV 数据为基础，围绕患者结构、入院与急诊、支付方式、离院去向、医院运营、疾病系统和病情风险开展分析。

系统先使用 Pandas 完成字段标准化、数值转换、缺失值处理和 SHA-256 增量去重，再将清洗后明细写入 MySQL。Spark 从 MySQL 读取明细数据，生成面向 BI 大屏的预聚合结果并回写 MySQL。Flask 通过参数化 SQL 向 Next.js + ECharts 前端提供 RESTful API；Redis 作为高频接口结果的缓存扩展。智能问答模块在受控工具调用范围内复用上述分析接口。

当前使用本系统的主要角色为数据分析人员、平台演示人员和项目开发维护人员。数据集为去标识化住院出院数据，不用于识别或追踪个人身份。

### 1.3 定义

| 术语 | 含义 |
|---|---|
| SPARCS | Statewide Planning and Research Cooperative System，纽约州住院出院数据来源体系；本项目使用其去标识化数据集。 |
| ETL | Extract, Transform, Load。指从 CSV 提取数据、按清洗规则转换、再写入数据库的过程。 |
| MySQL | 当前系统的核心关系型数据库，存储清洗后的明细数据和 Spark 预聚合结果。 |
| Redis | 内存键值数据库；当前作为 API 缓存扩展，不是业务数据的唯一来源。 |
| Spark | 当前以 Spark 4.0.1 单容器、`local[*]` 模式执行的离线批处理计算引擎。 |
| JDBC | Java Database Connectivity。Spark 通过 JDBC 从 MySQL 读取明细并写回汇总表。 |
| CCSR | Clinical Classifications Software Refined，临床诊断或操作分类编码。 |
| APR DRG | All Patient Refined Diagnosis Related Groups，按病例资源消耗和临床特征划分的诊断相关组。 |
| APR MDC | All Patient Refined Major Diagnostic Category，APR DRG 的上层主要诊断类别。 |
| CDM | Conceptual Data Model，概念数据模型，用于说明数据实体和关系。 |
| PDM | Physical Data Model，物理数据模型，用于说明实际表、字段、类型、索引和约束。 |

### 1.4 参考资料

- 《需求分析.md》
- 《数据清洗规则.md》
- 《数据库表结构说明.md》
- 《后端接口结构.md》
- 《前端展示结构.md》
- `docker-compose.yml`
- `sql/` 下的建表和索引脚本
- `spark_jobs/` 下的 Spark 汇总任务脚本

## 2. 数据存储架构设计

### 2.1 数据存储技术选型

| 存储/组件 | 当前版本 | 承担职责 | 选型说明 |
|---|---|---|---|
| MySQL | 8.0 | 明细数据和全部 BI 预聚合结果的主存储 | 支持结构化字段、唯一约束、索引、聚合结果查询和 Flask 参数化 SQL 访问。 |
| Redis | 7-alpine | 高频接口查询结果缓存 | 适合短生命周期 JSON 缓存，降低重复读取 MySQL 的压力；缓存失效后仍可从 MySQL 重建。 |
| Apache Spark | 4.0.1 | 批量聚合计算 | 通过 JDBC 读取 `inpatient_discharges` 并生成汇总表；Spark 是计算组件，不是主数据存储。 |
| CSV 文件 | 原始数据格式 | 原始数据输入与追溯 | 存放原始去标识化出院记录，清洗脚本读取后写入 MySQL。 |
| Docker Volume | `mysql_data`、`redis_data` | 容器数据持久化 | 容器重建后保留 MySQL 和 Redis 的持久化数据。 |

当前版本未部署独立列式数据库、Hive 数据仓库、HDFS、对象存储或 Spark Master/Worker 集群。数据量增长到单机 MySQL 或单机 Spark 难以承载时，可将离线明细和中间结果迁移到 Parquet + 对象存储/HDFS，并按需引入列式分析数据库。

### 2.2 数据存储部署视图

```text
原始 SPARCS 去标识化 CSV
        |
        | Pandas 清洗、标准化、SHA-256 去重
        v
MySQL 8.0 容器（medical-mysql，主存储）
        |-- inpatient_discharges：清洗后住院出院明细
        `-- 10 张 summary：Spark 预聚合结果
        ^                                      |
        | JDBC 读取/写回                         | 参数化 SQL 查询
        |                                      v
Spark 4.0.1 容器（medical-spark，local[*]）  Flask REST API
                                                |
                                                | 缓存命中/写入
                                                v
                                          Redis 容器（medical-redis）
                                                |
                                                v
                                     Next.js + ECharts BI 大屏 / Agent
```

部署约束如下：

- MySQL 容器使用命名卷 `mysql_data` 保存数据库文件，宿主机映射端口为 `3307`。
- Redis 容器使用命名卷 `redis_data`，启用 AOF 持久化，宿主机映射端口为 `6379`。
- Spark 容器挂载 `./data` 至 `/app/data`，挂载 `./spark_jobs` 至 `/app/spark_jobs`，仅用于运行当前批处理任务。
- 前端不直接连接 MySQL 或 Redis；所有数据访问通过 Flask API 完成。

## 3. 数据存储详细设计

### 3.1 结构化数据库

#### 3.1.1 类型划分

MySQL 数据库 `medical_platform` 的表分为两类：

1. 明细主表：`inpatient_discharges`，一行对应一条清洗后的住院出院记录，是所有分析的唯一业务事实来源。
2. 预聚合结果表：由 Spark 从主表按不同分析维度生成，服务 BI 图表和 Agent 工具调用，避免 API 对数百万明细记录重复做实时聚合。

所有汇总表保留 `hospital_service_area`、`hospital_county`、`facility_name` 三个全局筛选字段和 `updated_at`，保证区域、县、医院三个筛选条件可联动到全部图表。

#### 3.1.2 标识符和约定

| 约定项 | 规则 |
|---|---|
| 表名 | 全小写蛇形命名，明细表使用业务实体名称，汇总表统一以 `_summary` 结尾。 |
| 字段名 | 全小写蛇形命名；计数使用 `_count`，金额或时长汇总使用 `_sum`。 |
| 主键 | 明细表使用 `BIGINT UNSIGNED` 自增主键 `id`。当前汇总表以业务分组字段作为逻辑唯一组合。 |
| 增量去重 | `source_row_hash` 保存清洗后业务字段的 SHA-256 指纹，并建立唯一约束防止重复导入。 |
| 编码字段 | 医疗机构 ID、APR DRG、APR MDC 等需保留前导零的值使用 `VARCHAR`，不转为数值。 |
| 金额字段 | `total_charges`、`total_costs`、`charge_cost_gap` 使用 `DECIMAL`，避免浮点金额精度误差。 |
| 时间字段 | 明细入库使用 `created_at`；汇总任务生成时间使用 `updated_at`。 |
| 描述字段 | CCSR/APR 的 description 用于展示，不作为疾病分析的主分组键；聚合以对应 code 为准。 |

#### 3.1.3 数据库表设计格式说明

##### 3.1.3.1 住院患者出院明细表（`inpatient_discharges`）

该表保存清洗后的原子记录。完整字段定义见《数据库表结构说明.md》，核心字段如下。

| 字段中文名 | 字段名 | 字段类型 | 非空 | 主键/约束 | 说明 |
|---|---|---:|---|---|---|
| 自增主键 | `id` | BIGINT UNSIGNED | 是 | 主键 | 明细记录唯一标识。 |
| 源行指纹 | `source_row_hash` | CHAR(64) | 是 | 唯一 | 清洗后业务字段的 SHA-256 指纹，用于增量去重。 |
| 源文件名 | `source_file_name` | VARCHAR(255) | 是 | - | 支持原始数据追溯。 |
| 源文件行号 | `source_row_number` | BIGINT UNSIGNED | 是 | - | 支持定位源记录。 |
| 医院服务区域 | `hospital_service_area` | VARCHAR(100) | 否 | 索引维度 | 医院区域筛选字段。 |
| 医院所在县 | `hospital_county` | VARCHAR(100) | 否 | 索引维度 | 医院县级筛选字段。 |
| 医疗机构名称 | `facility_name` | VARCHAR(255) | 是 | 索引维度 | 医院筛选与展示字段。 |
| 年龄组 | `age_group` | VARCHAR(30) | 否 | - | 患者年龄区间。 |
| 年龄排序值 | `age_group_sort` | TINYINT UNSIGNED | 否 | - | 保证年龄图表正确排序。 |
| 住院时长 | `length_of_stay` | DECIMAL(10,2) | 否 | - | 单位为天。 |
| 入院类型 | `type_of_admission` | VARCHAR(50) | 否 | - | 用于入院结构分析。 |
| 急诊标记 | `emergency_department_indicator` | CHAR(1) | 否 | - | 标准值为 `Y`、`N` 或空。 |
| CCSR 诊断编码 | `ccsr_diagnosis_code` | VARCHAR(30) | 否 | - | 具体诊断分析编码。 |
| APR DRG 编码 | `apr_drg_code` | VARCHAR(30) | 否 | - | 病例组编码。 |
| APR MDC 编码 | `apr_mdc_code` | VARCHAR(30) | 否 | - | 疾病系统编码。 |
| 病情严重程度 | `apr_severity_code` | TINYINT UNSIGNED | 否 | - | 正常范围为 1 至 4。 |
| 死亡风险 | `apr_risk_of_mortality` | TINYINT UNSIGNED | 否 | - | 正常范围为 1 至 4。 |
| 主要支付方式 | `payment_typology_1` | VARCHAR(100) | 否 | - | 支付结构分析字段。 |
| 总收费 | `total_charges` | DECIMAL(16,2) | 否 | - | 医疗总收费金额。 |
| 总成本 | `total_costs` | DECIMAL(16,2) | 否 | - | 医院总成本金额。 |
| 入库时间 | `created_at` | TIMESTAMP | 是 | - | 明细写入时间。 |

##### 3.1.3.2 患者分析汇总表

| 序号 | 表名 | 分组维度 | 服务功能 |
|---:|---|---|---|
| 1 | `patient_age_gender_summary` | 年龄组、年龄排序值、性别 | 年龄与性别结构。 |
| 2 | `patient_payment_summary` | 主要支付方式 | 支付方式分布、收费和成本分析。 |
| 3 | `patient_disposition_summary` | 原始离院去向 | 离院去向 Top N。 |
| 4 | `patient_admission_emergency_summary` | 入院类型、急诊标记 | 入院与急诊结构。 |
| 5 | `patient_medical_surgical_summary` | 内科/外科分类 | 内外科出院量、费用和住院时长对比。 |

##### 3.1.3.3 医院运营汇总表（`hospital_operation_summary`）

该表按区域、县和医院名称聚合，服务核心 KPI、医院资源与成本气泡图、运营排名和医院对比表。

| 字段中文名 | 字段名 | 字段类型 | 说明 |
|---|---|---|---|
| 医院服务区域 | `hospital_service_area` | VARCHAR(100) | 全局筛选维度。 |
| 医院所在县 | `hospital_county` | VARCHAR(100) | 全局筛选和对比展示字段。 |
| 医疗机构名称 | `facility_name` | VARCHAR(255) | 医院名称及全局筛选维度。 |
| 出院记录数 | `discharge_count` | BIGINT | 当前医院的出院量。 |
| 住院天数总和 | `length_of_stay_sum` | DECIMAL/DOUBLE | 计算平均住院天数的分子。 |
| 总收费 | `total_charges_sum` | DECIMAL/DOUBLE | 当前医院总收费。 |
| 总成本 | `total_costs_sum` | DECIMAL/DOUBLE | 当前医院总成本。 |
| 急诊收费/成本 | `emergency_*_sum` | DECIMAL/DOUBLE | 急诊记录的收费和成本汇总。 |
| 非急诊收费/成本 | `non_emergency_*_sum` | DECIMAL/DOUBLE | 非急诊记录的收费和成本汇总。 |
| 急诊有效记录数 | `emergency_yes_count`、`emergency_known_count` | BIGINT | 计算急诊占比。 |
| 汇总时间 | `updated_at` | TIMESTAMP | Spark 任务生成时间。 |

平均住院天数、平均收费、平均成本均应使用“同一筛选范围下的总和 / 出院记录总数”计算，不能直接对医院级平均值再次取平均。

##### 3.1.3.4 疾病与风险汇总表

| 序号 | 表名 | 分组编码 | 服务功能 |
|---:|---|---|---|
| 1 | `disease_system_summary` | `apr_mdc_code` | 疾病系统分布。 |
| 2 | `diagnosis_summary` | `ccsr_diagnosis_code` | 高发疾病 Top N。 |
| 3 | `patient_risk_summary` | `apr_severity_code`、`apr_risk_of_mortality` | 病情严重程度和死亡风险分析。 |
| 4 | `disease_drilldown_summary` | `apr_mdc_code`、`apr_drg_code`、`ccsr_diagnosis_code` | MDC → DRG → CCSR 三级疾病下钻。 |

上述表均同时保存 description 展示字段、`discharge_count`、`length_of_stay_sum`、`total_charges_sum`、`total_costs_sum` 和 `updated_at`。描述字段不参与主要分组，仅用于使编码可读。

#### 3.1.4 支持软件

- MySQL Server 8.0
- Apache Spark 4.0.1
- Python（Pandas 数据清洗、PySpark 聚合）
- Flask（数据查询 API）
- Docker Compose（MySQL、Redis、Spark 容器编排）

### 3.2 非结构化数据库

当前系统部署 Redis 7-alpine，用作缓存型键值存储，不承担住院明细或预聚合数据的唯一持久化职责。缓存键应使用统一前缀并包含筛选条件的规范化表示，例如：

```text
medical:dashboard:<endpoint>:<filters_hash>
medical:agent:<tool_name>:<filters_hash>
```

缓存值为 Flask API 返回的 JSON 数据。缓存键必须包含 `hospital_service_area`、`hospital_county`、`facility_name` 和接口特有参数（如 Top N 的 `limit`、医院排名的排序字段），避免不同筛选条件错误复用同一结果。

建议缓存策略如下：

| 项目 | 设计规则 |
|---|---|
| 读取流程 | 先查询 Redis；未命中时查询 MySQL，再将结果写入 Redis。 |
| 失效时间 | 由环境变量或应用配置统一管理；展示性汇总接口宜使用短 TTL。 |
| 主动失效 | Spark 全量重建汇总表成功后，清理 `medical:dashboard:*` 和相关 Agent 缓存。 |
| 数据一致性 | MySQL 始终为权威数据源；缓存异常、过期或清空后可完整重建。 |
| 持久化 | Redis 已启用 AOF，并使用 `redis_data` Docker 卷；该持久化不替代 MySQL 备份。 |

### 3.3 列式数据库

当前版本未部署 ClickHouse、Doris、Hive 等独立列式数据库或数据仓库，也未将 Parquet 作为生产数据源。Spark 在执行 DataFrame 聚合时采用列式执行优化，但这不等同于项目已部署列式数据库。

后续当明细数据年份增加、查询维度扩大或单机 MySQL 聚合性能不足时，可采用以下扩展路线：

1. 将原始 CSV 和清洗后中间数据落为按出院年份分区的 Parquet 文件。
2. 将 Spark 批处理改为读取 Parquet，并将大屏热点聚合结果继续回写 MySQL 或写入列式分析库。
3. 对高并发、跨年度、多维分析引入 ClickHouse 或 Apache Doris，MySQL 仍可保留配置和应用事务数据。

### 3.4 分布式文件系统存储

当前版本未部署 HDFS、OSS、S3 或其他对象存储。原始 CSV 由本地项目 `data/` 目录或用户指定的本地文件路径提供；Spark 容器通过 `./data:/app/data` 挂载访问数据。MySQL 和 Redis 的持久化由 Docker 命名卷完成。

若后续接入多年份大规模数据、多人协作导入或需要长期保存原始版本，可采用对象存储或 HDFS，并按以下层次管理：

```text
raw/        原始 CSV，只读保留
clean/      清洗后的标准化数据，建议 Parquet 分区
aggregate/  Spark 批处理的中间结果或离线备份
archive/    历史数据与导入版本归档
```

此扩展不会改变当前 MySQL 面向 Flask API 提供预聚合结果的访问方式。

## 4. 结构设计

### 4.1 结构化数据库

#### 4.1.1 概念模型

系统以住院出院记录为事实主体。医院、患者人口学特征、入院信息、疾病编码、严重程度、支付方式、费用和离院去向都是该事实记录的分析维度。当前采用单张清洗后明细事实表，以避免在原始去标识化数据中人为拆分后产生不必要的关联复杂度。

```text
医院维度 ─┐
患者维度 ─┤
入院维度 ─┤
疾病与风险维度 ─┼── inpatient_discharges（住院出院明细事实）
支付与费用维度 ─┤
离院去向维度 ─┘
                     |
                     | Spark 批量聚合
                     v
         患者分析 / 医院运营 / 疾病风险 10 张 summary 表
```

#### 4.1.2 物理模型

```text
inpatient_discharges
    |
    +-- patient_age_gender_summary
    +-- patient_payment_summary
    +-- patient_disposition_summary
    +-- patient_admission_emergency_summary
    +-- patient_medical_surgical_summary
    +-- hospital_operation_summary
    +-- disease_system_summary
    +-- diagnosis_summary
    +-- patient_risk_summary
    `-- disease_drilldown_summary
```

`inpatient_discharges` 是数据清洗的唯一入库目标。各汇总表均为可再生数据，Spark 任务重跑时按明细重新计算和写入；不得将汇总表作为新的事实数据源再次汇总，以防止重复计数。

物理索引以数据追溯、增量去重和高频筛选为重点：

- `source_row_hash` 使用唯一索引，保障重复导入时的幂等性。
- 明细及汇总查询使用 `hospital_service_area`、`hospital_county`、`facility_name` 作为核心筛选维度。
- 汇总表应结合其业务分组字段建立联合索引，例如支付方式、疾病编码、入院类型或医院名称。
- API 排名、分页与模糊匹配使用白名单排序字段和参数化 SQL，避免直接拼接查询条件。

### 4.2 非结构化数据库

Redis 的逻辑结构为“命名空间 + 接口/工具名称 + 参数哈希”的键值模型。

| 项目 | 设计 |
|---|---|
| Key | `medical:dashboard:<endpoint>:<filters_hash>` 或 `medical:agent:<tool_name>:<filters_hash>`。 |
| Value | UTF-8 JSON，保存 API 返回的数据主体和必要元信息。 |
| TTL | 按接口配置，不作为永久存档。 |
| 写入方 | Flask API 在 MySQL 查询成功后写入。 |
| 读取方 | Flask API 与 Agent 工具调用。 |
| 失效方 | Spark 汇总任务成功后的维护脚本或应用缓存管理逻辑。 |

当前不将用户个人信息、原始 CSV 文件或数据库密码写入 Redis。

### 4.3 列式数据库

当前物理模型不适用：未部署独立列式数据库、Hive 表或 Parquet 数据仓库。若未来引入列式存储，建议以 `discharge_year` 为基础分区字段，并将 `hospital_service_area`、`hospital_county`、`apr_mdc_code` 等常用筛选字段用于分区或排序设计；具体设计须根据数据规模和实际查询日志重新评估。

### 4.4 分布式文件系统存储

当前物理模型不适用：项目没有 HDFS 或对象存储集群。现阶段文件与卷的职责如下：

| 存储位置 | 内容 | 约束 |
|---|---|---|
| `data/` 或用户指定本地路径 | 原始 CSV 数据 | 作为导入源，不由前端直接读取。 |
| `spark_jobs/` | Spark 聚合脚本 | 以只读挂载方式提供给 Spark 容器执行。 |
| `mysql_data` | MySQL 数据文件 | Docker 命名卷，需纳入备份策略。 |
| `redis_data` | Redis AOF 与缓存持久化文件 | Docker 命名卷，不替代权威数据备份。 |

## 5. 运用设计（可选）

### 5.1 数据字典设计

数据字典包括数据类型、枚举值和计算口径。详细字段级说明以《数据库表结构说明.md》为准。

#### 5.1.1 数据类型

| 数据类型编码 | 数据类型 | 用途 |
|---|---|---|
| `DT_01` | BIGINT UNSIGNED | 明细主键、源行号和出院记录计数，满足大数据量增长。 |
| `DT_02` | VARCHAR(n) | 医院名称、区域、医疗编码、描述和分类字段；编码字段可保留前导零。 |
| `DT_03` | DECIMAL(p,2) | 住院天数、费用、成本、出生体重等需要固定精度的数值。 |
| `DT_04` | TINYINT UNSIGNED | 年龄排序、严重程度、死亡风险等小范围编码。 |
| `DT_05` | CHAR(1) | 急诊标记等单字符状态。 |
| `DT_06` | DATETIME/TIMESTAMP | 入库时间和 Spark 汇总生成时间。 |
| `DT_07` | CHAR(64) | SHA-256 源行指纹。 |

#### 5.1.2 主要枚举与计算口径

| 字典编码 | 字段 | 取值/计算规则 |
|---|---|---|
| `DD_001` | `emergency_department_indicator` | `Y` 表示急诊，`N` 表示非急诊，空值表示未知；急诊占比的分母只统计 `Y` 或 `N`。 |
| `DD_002` | `zip_region_type` | `LOCAL`、`OUT_OF_STATE`、`UNKNOWN`。 |
| `DD_003` | `is_facility_redacted` | `1` 为医院信息脱敏占位记录，`0` 为正常记录。 |
| `DD_004` | `apr_severity_code` | 病情严重程度编码，正常范围 1 至 4。 |
| `DD_005` | `apr_risk_of_mortality` | 死亡风险编码，正常范围 1 至 4。 |
| `DD_006` | `apr_medical_surgical_description` | 标准分析值为 `Medical` 或 `Surgical`。 |
| `DD_007` | 金额差额 | `charge_cost_gap = total_charges - total_costs`。 |
| `DD_008` | 汇总平均值 | `SUM(指标总和) / SUM(discharge_count)`；筛选后不得对分组平均值直接再平均。 |

### 5.2 数据字典备注安全保密设计

1. 数据源为去标识化住院出院数据。系统不应尝试通过邮编、医院、疾病等组合反向识别个人，也不得将数据用于个体级决策。
2. 医院脱敏记录以 `is_facility_redacted` 标识。医院区域或县等关键上下文缺失、无法支持医院比较的记录，不应进入依赖医院空间维度的分析口径。
3. 数据库连接信息、LLM API Key 等敏感配置仅放在本地 `.env` 文件中；`.env` 和 `.env.*` 必须被 Git 忽略，`.env.example` 仅提供无密钥配置模板。
4. Flask 接口使用参数化 SQL、排序字段白名单和请求参数校验，防止 SQL 注入及无效分页、排序请求。
5. Agent 仅调用预定义的分析工具/API，不允许模型自由拼接 SQL 或直接访问数据库凭据。
6. MySQL 生产部署应为应用账户分配最小权限，禁止应用使用 root 账户；开发环境默认口令不得迁移至生产环境。
7. 应定期备份 `mysql_data`，并验证恢复流程。Redis 缓存可清除后重建，不能作为唯一备份。
8. 当前项目尚未实现登录、RBAC 和操作审计。若作为真实医疗生产系统上线，应补充 TLS、身份认证、角色权限、操作审计、密钥轮换、备份加密和数据保留/销毁制度。
