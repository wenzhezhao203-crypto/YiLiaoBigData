# 智慧医疗住院数据分析平台数据存储说明文档

## 1. 引言

### 1.1 编写目的

本文档说明智慧医疗住院数据分析平台中 MySQL 关系型数据库的存储设计，包括数据库选型、部署、表结构、命名约定、索引策略、数据字典和安全要求。

本文档作为数据清洗、数据分析、后端接口、BI 大屏开发和测试的共同依据，供开发人员、测试人员、数据分析人员和运维人员参考。

### 1.2 背景

智慧医疗住院数据分析平台以 SPARCS 去标识化住院出院 CSV 数据为基础，对患者结构、入院与急诊、支付方式、离院去向、医院运营、疾病系统和病情风险进行分析。

原始数据经过清洗、标准化和去重后写入 MySQL 明细表。分析任务基于明细表生成面向 BI 大屏的预聚合结果，并回写 MySQL；Flask 后端通过参数化 SQL 查询 MySQL 后向 Next.js + ECharts 前端提供 RESTful API。MySQL 是当前系统唯一的业务数据存储和查询数据源。

### 1.3 定义

| 术语 | 含义 |
|---|---|
| SPARCS | Statewide Planning and Research Cooperative System，纽约州住院出院数据来源体系；本项目使用其去标识化数据集。 |
| ETL | Extract, Transform, Load。指从 CSV 提取数据、按清洗规则转换、再写入 MySQL 的过程。 |
| MySQL | 当前系统唯一的关系型数据存储，用于保存清洗后明细和预聚合结果。 |
| JDBC | Java Database Connectivity。分析任务通过 JDBC 从 MySQL 读取明细并写回汇总表。 |
| CCSR | Clinical Classifications Software Refined，临床诊断或操作分类编码。 |
| APR DRG | All Patient Refined Diagnosis Related Groups，按病例资源消耗和临床特征划分的诊断相关组。 |
| APR MDC | All Patient Refined Major Diagnostic Category，APR DRG 的上层主要诊断类别。 |
| CDM | Conceptual Data Model，概念数据模型，用于说明数据实体和关系。 |
| PDM | Physical Data Model，物理数据模型，用于说明实际表、字段、类型、索引和约束。 |

### 1.4 参考资料

- 《需求分析.md》
- 《数据清洗规则.md》
- 《数据库表结构说明.md》
- 《后端接口结构.md》
- 《前端展示结构.md》
- `docker-compose.yml`
- `sql/` 下的建表和索引脚本

## 2. 数据存储架构设计

### 2.1 数据存储技术选型

系统使用 MySQL 8.0 作为结构化数据库，负责保存清洗后的住院出院明细数据和全部 BI 预聚合结果。

| 选型项 | 当前配置 | 承担职责 | 选型说明 |
|---|---|---|---|
| 数据库 | MySQL 8.0 | 主数据存储 | 支持结构化字段、唯一约束、索引、事务和参数化 SQL 查询。 |
| 数据库名称 | `medical_platform` | 业务数据库 | 统一保存明细表和汇总表。 |
| 数据持久化 | Docker 命名卷 `mysql_data` | 数据文件持久化 | 容器重建后仍可保留 MySQL 数据。 |
| 字符集 | `utf8mb4` | 中文及医疗描述字段存储 | 支持多语言文本和完整字符集。 |

选择 MySQL 的原因如下：

1. 住院出院数据和预聚合结果具有明确字段、固定类型和稳定的分组关系，适合关系型表存储。
2. BI 大屏的查询主要为区域、县、医院等多条件筛选，以及 Top N、排序和分页，适合通过索引和 SQL 聚合支持。
3. 明细导入需要基于源行指纹实现增量去重，MySQL 唯一约束能够保证重复导入时的数据幂等性。
4. Flask 后端可通过成熟的 MySQL 驱动和参数化 SQL 直接访问，降低开发和运维复杂度。

### 2.2 数据存储部署视图

```text
原始 SPARCS 去标识化 CSV
        |
        | 数据清洗、标准化、SHA-256 去重
        v
MySQL 8.0（medical_platform）
        |
        |-- inpatient_discharges：清洗后住院出院明细
        |
        `-- 10 张 summary：预聚合分析结果
                |
                | 参数化 SQL 查询
                v
          Flask REST API → Next.js + ECharts BI 大屏
```

部署约束如下：

- MySQL 使用 Docker 容器 `medical-mysql` 运行，镜像版本为 `mysql:8.0`。
- 宿主机端口 `3307` 映射到容器内 MySQL 端口 `3306`。
- 数据目录使用 Docker 命名卷 `mysql_data` 持久化。
- 前端不直接访问数据库，所有查询必须经过 Flask 后端接口。

## 3. 数据存储详细设计

### 3.1 结构化数据库

#### 3.1.1 类型划分

MySQL 数据库 `medical_platform` 中的业务表分为两类：

1. 明细主表：`inpatient_discharges`，一行对应一条清洗后的住院出院记录，是全部分析的事实数据来源。
2. 预聚合结果表：按照不同分析维度生成，直接服务 BI 图表与智能问答工具查询，避免接口重复扫描全部明细记录。

所有汇总表均保留 `hospital_service_area`、`hospital_county`、`facility_name` 三个全局筛选字段及 `updated_at`，保证区域、县、医院三个筛选条件可以联动到所有图表。

#### 3.1.2 标识符和约定

| 约定项 | 规则 |
|---|---|
| 表名 | 全小写蛇形命名；汇总表统一以 `_summary` 结尾。 |
| 字段名 | 全小写蛇形命名；计数使用 `_count`，金额或时长汇总使用 `_sum`。 |
| 主键 | 明细表使用 `BIGINT UNSIGNED` 自增主键 `id`。汇总表以业务分组字段构成逻辑唯一组合。 |
| 增量去重 | `source_row_hash` 保存清洗后业务字段的 SHA-256 指纹，并建立唯一约束。 |
| 编码字段 | 医疗机构 ID、APR DRG、APR MDC 等须保留前导零的值使用 `VARCHAR`，不得转为数值。 |
| 金额字段 | `total_charges`、`total_costs`、`charge_cost_gap` 使用 `DECIMAL`，避免浮点精度误差。 |
| 时间字段 | 明细入库使用 `created_at`；汇总结果生成时间使用 `updated_at`。 |
| 描述字段 | CCSR/APR 描述字段只负责前端展示；疾病聚合以对应编码字段为准。 |

#### 3.1.2.1 数据库表清单

数据库表名采用英文小写蛇形命名。明细表保存清洗后的住院出院记录，汇总表保存面向 BI 大屏和接口查询的预聚合结果。

| 序号 | 表名 | 描述 | 说明 |
|---:|---|---|---|
| 1 | `inpatient_discharges` | 住院患者出院明细表 | 保存每条清洗后的住院出院记录，是所有分析的事实数据来源。 |
| 2 | `patient_age_gender_summary` | 患者年龄性别汇总表 | 支持年龄组与性别结构分析。 |
| 3 | `patient_payment_summary` | 患者支付方式汇总表 | 支持主要支付方式、收费和成本分析。 |
| 4 | `patient_disposition_summary` | 患者离院去向汇总表 | 支持离院去向统计和 Top N 展示。 |
| 5 | `patient_admission_emergency_summary` | 入院与急诊汇总表 | 支持入院类型和急诊/非急诊结构分析。 |
| 6 | `patient_medical_surgical_summary` | 内外科汇总表 | 支持内科与外科出院量、费用和住院时长比较。 |
| 7 | `hospital_operation_summary` | 医院运营汇总表 | 支持核心运营指标、医院排名、资源与成本分析。 |
| 8 | `disease_system_summary` | 疾病系统汇总表 | 支持 APR MDC 疾病系统分布展示。 |
| 9 | `diagnosis_summary` | 高发疾病汇总表 | 支持 CCSR 诊断 Top N 分析。 |
| 10 | `patient_risk_summary` | 病情风险汇总表 | 支持病情严重程度和死亡风险分布分析。 |
| 11 | `disease_drilldown_summary` | 疾病分层下钻汇总表 | 支持 APR MDC → APR DRG → CCSR 三级疾病下钻。 |

#### 3.1.3 数据库表设计格式说明

##### 3.1.3.1 住院患者出院明细表（`inpatient_discharges`）

该表保存清洗后的原子记录，是所有预聚合表的唯一来源。完整字段定义见《数据库表结构说明.md》，核心字段如下。

| 字段中文名 | 字段名 | 字段类型 | 非空 | 主键/约束 | 说明 |
|---|---|---:|---|---|---|
| 自增主键 | `id` | BIGINT UNSIGNED | 是 | 主键 | 明细记录唯一标识。 |
| 源行指纹 | `source_row_hash` | CHAR(64) | 是 | 唯一 | 清洗后业务字段的 SHA-256 指纹，用于增量去重。 |
| 源文件名 | `source_file_name` | VARCHAR(255) | 是 | - | 支持原始数据追溯。 |
| 源文件行号 | `source_row_number` | BIGINT UNSIGNED | 是 | - | 支持定位源记录。 |
| 医院服务区域 | `hospital_service_area` | VARCHAR(100) | 否 | 筛选维度 | 医院区域筛选字段。 |
| 医院所在县 | `hospital_county` | VARCHAR(100) | 否 | 筛选维度 | 医院县级筛选字段。 |
| 医疗机构名称 | `facility_name` | VARCHAR(255) | 是 | 筛选维度 | 医院筛选与展示字段。 |
| 年龄组 | `age_group` | VARCHAR(30) | 否 | - | 患者年龄区间。 |
| 年龄排序值 | `age_group_sort` | TINYINT UNSIGNED | 否 | - | 保证年龄图表正确排序。 |
| 住院时长 | `length_of_stay` | DECIMAL(10,2) | 否 | - | 单位为天。 |
| 入院类型 | `type_of_admission` | VARCHAR(50) | 否 | - | 用于入院结构分析。 |
| 急诊标记 | `emergency_department_indicator` | CHAR(1) | 否 | - | 标准值为 `Y`、`N` 或空。 |
| 离院去向 | `patient_disposition` | VARCHAR(100) | 否 | - | 原始离院去向。 |
| CCSR 诊断编码 | `ccsr_diagnosis_code` | VARCHAR(30) | 否 | - | 具体诊断分析编码。 |
| APR DRG 编码 | `apr_drg_code` | VARCHAR(30) | 否 | - | 病例组编码。 |
| APR MDC 编码 | `apr_mdc_code` | VARCHAR(30) | 否 | - | 疾病系统编码。 |
| 病情严重程度 | `apr_severity_code` | TINYINT UNSIGNED | 否 | - | 正常范围为 1 至 4。 |
| 死亡风险 | `apr_risk_of_mortality` | TINYINT UNSIGNED | 否 | - | 正常范围为 1 至 4。 |
| 主要支付方式 | `payment_typology_1` | VARCHAR(100) | 否 | - | 支付结构分析字段。 |
| 总收费 | `total_charges` | DECIMAL(16,2) | 否 | - | 医疗总收费金额。 |
| 总成本 | `total_costs` | DECIMAL(16,2) | 否 | - | 医院总成本金额。 |
| 入库时间 | `created_at` | TIMESTAMP | 是 | - | 明细写入时间。 |

> 上表此前仅列示了核心字段，以下为该表的其余字段；两部分合并后构成 `inpatient_discharges` 的完整表结构。

| 字段中文名 | 字段名 | 字段类型 | 非空 | 说明 |
|---|---|---|---|---|
| 医院运营证书编号 | `operating_certificate_number` | VARCHAR(30) | 否 | 按字符串保存。 |
| 医疗机构永久 ID | `permanent_facility_id` | VARCHAR(30) | 否 | 保留前导零。 |
| 医院脱敏标记 | `is_facility_redacted` | TINYINT(1) | 否 | `1` 表示脱敏医院占位记录，`0` 表示正常记录。 |
| 三位邮编 | `zip_code_3` | VARCHAR(10) | 否 | 三位邮编或 `OOS` 等特殊值。 |
| 邮编类型 | `zip_region_type` | VARCHAR(30) | 否 | `LOCAL`、`OUT_OF_STATE` 或 `UNKNOWN`。 |
| 性别 | `gender` | VARCHAR(30) | 否 | 标准化性别。 |
| 种族 | `race` | VARCHAR(100) | 否 | 患者种族。 |
| 民族 | `ethnicity` | VARCHAR(100) | 否 | 患者民族。 |
| 离院去向分组 | `disposition_group` | VARCHAR(50) | 否 | 预留的标准化分组字段。 |
| 出院年份 | `discharge_year` | SMALLINT UNSIGNED | 否 | 出院年份。 |
| CCSR 诊断描述 | `ccsr_diagnosis_description` | VARCHAR(500) | 否 | CCSR 诊断编码对应描述。 |
| CCSR 操作编码 | `ccsr_procedure_code` | VARCHAR(30) | 否 | 部分记录无操作，允许为空。 |
| CCSR 操作描述 | `ccsr_procedure_description` | VARCHAR(500) | 否 | 操作编码对应描述，允许为空。 |
| APR DRG 描述 | `apr_drg_description` | VARCHAR(500) | 否 | APR DRG 编码对应描述。 |
| APR MDC 描述 | `apr_mdc_description` | VARCHAR(500) | 否 | APR MDC 编码对应疾病系统描述。 |
| 病情严重程度描述 | `apr_severity_description` | VARCHAR(50) | 否 | 病情严重程度文字描述。 |
| 内科/外科分类 | `apr_medical_surgical_description` | VARCHAR(50) | 否 | `Medical`、`Surgical` 等分类。 |
| 次要支付方式 | `payment_typology_2` | VARCHAR(100) | 否 | 可为空。 |
| 第三支付方式 | `payment_typology_3` | VARCHAR(100) | 否 | 可为空。 |
| 出生体重 | `birth_weight` | DECIMAL(10,2) | 否 | 仅新生儿入院记录保留。 |
| 收费成本差额 | `charge_cost_gap` | DECIMAL(16,2) | 否 | 总收费减总成本。 |

##### 3.1.3.2 年龄与性别汇总表（`patient_age_gender_summary`）

按区域、县、医院、年龄组和性别聚合，用于年龄与性别结构图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，全局筛选维度。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，全局筛选维度。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称，全局筛选维度。 |
| `age_group` | VARCHAR(30) | 年龄组。 |
| `age_group_sort` | SMALLINT | 年龄组排序值。 |
| `gender` | VARCHAR(30) | 性别。 |
| `discharge_count` | BIGINT | 该年龄组与性别组合的出院记录数。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.3 支付方式汇总表（`patient_payment_summary`）

按主要支付方式聚合，用于支付方式分布图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | LONGTEXT | 医院服务区域，全局筛选维度。 |
| `hospital_county` | LONGTEXT | 医院所在县，全局筛选维度。 |
| `facility_name` | LONGTEXT | 医疗机构名称，全局筛选维度。 |
| `payment_typology_1` | LONGTEXT | 主要支付方式。 |
| `discharge_count` | BIGINT | 对应支付方式的出院记录数。 |
| `total_charges_sum` | DECIMAL(26,2) | 对应支付方式的总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 对应支付方式的总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.4 离院去向汇总表（`patient_disposition_summary`）

按原始离院去向聚合，用于离院去向 Top N 图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | LONGTEXT | 医院服务区域，全局筛选维度。 |
| `hospital_county` | LONGTEXT | 医院所在县，全局筛选维度。 |
| `facility_name` | LONGTEXT | 医疗机构名称，全局筛选维度。 |
| `patient_disposition` | LONGTEXT | 原始离院去向。 |
| `discharge_count` | BIGINT | 对应离院去向的出院记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `total_charges_sum` | DECIMAL(26,2) | 总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.5 入院与急诊汇总表（`patient_admission_emergency_summary`）

按入院类型和急诊标记聚合，用于入院与急诊结构图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | LONGTEXT | 医院服务区域，全局筛选维度。 |
| `hospital_county` | LONGTEXT | 医院所在县，全局筛选维度。 |
| `facility_name` | LONGTEXT | 医疗机构名称，全局筛选维度。 |
| `type_of_admission` | LONGTEXT | 入院类型。 |
| `emergency_department_indicator` | LONGTEXT | 急诊标记：`Y`、`N` 或空。 |
| `discharge_count` | BIGINT | 该入院类型和急诊组合的记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.6 内外科汇总表（`patient_medical_surgical_summary`）

按内科/外科分类聚合，用于内外科比例和费用比较图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | LONGTEXT | 医院服务区域，全局筛选维度。 |
| `hospital_county` | LONGTEXT | 医院所在县，全局筛选维度。 |
| `facility_name` | LONGTEXT | 医疗机构名称，全局筛选维度。 |
| `apr_medical_surgical_description` | LONGTEXT | 内科/外科分类，例如 `Medical`、`Surgical`。 |
| `discharge_count` | BIGINT | 对应分类的出院记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `total_charges_sum` | DECIMAL(26,2) | 总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.7 医院运营汇总表（`hospital_operation_summary`）

按区域、县和医院名称聚合，服务核心 KPI、医院资源与成本、运营排名和医院对比。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，全局筛选维度。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，全局筛选与展示字段。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称，全局筛选与展示字段。 |
| `discharge_count` | BIGINT | 当前医院出院记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `total_charges_sum` | DECIMAL(26,2) | 总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 总成本。 |
| `charge_cost_gap_sum` | DECIMAL(26,2) | 总收费减总成本的差额总和。 |
| `emergency_charges_sum` | DECIMAL(26,2) | 急诊记录总收费。 |
| `non_emergency_charges_sum` | DECIMAL(26,2) | 非急诊记录总收费。 |
| `emergency_costs_sum` | DECIMAL(26,2) | 急诊记录总成本。 |
| `non_emergency_costs_sum` | DECIMAL(26,2) | 非急诊记录总成本。 |
| `emergency_yes_count` | BIGINT | 急诊标记为 `Y` 的记录数。 |
| `emergency_known_count` | BIGINT | 急诊标记为 `Y` 或 `N` 的有效记录数。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.8 疾病系统汇总表（`disease_system_summary`）

按 APR MDC 疾病系统编码聚合，用于疾病系统分布图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，全局筛选维度。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，全局筛选维度。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称，全局筛选维度。 |
| `apr_mdc_code` | VARCHAR(30) | APR MDC 疾病系统编码，分组字段。 |
| `apr_mdc_description` | VARCHAR(500) | APR MDC 疾病系统描述，仅展示使用。 |
| `discharge_count` | BIGINT | 该疾病系统的出院记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `total_charges_sum` | DECIMAL(26,2) | 总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.9 高发疾病汇总表（`diagnosis_summary`）

按 CCSR 诊断编码聚合，用于高发疾病 Top N 图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，全局筛选维度。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，全局筛选维度。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称，全局筛选维度。 |
| `ccsr_diagnosis_code` | VARCHAR(30) | CCSR 具体诊断编码，分组字段。 |
| `ccsr_diagnosis_description` | VARCHAR(500) | CCSR 诊断描述，仅展示使用。 |
| `discharge_count` | BIGINT | 该具体诊断的出院记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `total_charges_sum` | DECIMAL(26,2) | 总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.10 病情风险汇总表（`patient_risk_summary`）

按病情严重程度和死亡风险编码聚合，用于病情严重程度分布图表。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，全局筛选维度。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，全局筛选维度。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称，全局筛选维度。 |
| `apr_severity_code` | SMALLINT | 病情严重程度编码，分组字段。 |
| `apr_severity_description` | VARCHAR(50) | 病情严重程度描述，仅展示使用。 |
| `apr_risk_of_mortality` | SMALLINT | 死亡风险编码，分组字段。 |
| `discharge_count` | BIGINT | 该风险组合的出院记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `total_charges_sum` | DECIMAL(26,2) | 总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

##### 3.1.3.11 疾病分层下钻汇总表（`disease_drilldown_summary`）

按 APR MDC、APR DRG、CCSR Diagnosis 三层编码聚合，用于 MDC → DRG → CCSR 三级疾病下钻。

| 字段 | 类型 | 含义 |
|---|---|---|
| `hospital_service_area` | VARCHAR(100) | 医院服务区域，全局筛选维度。 |
| `hospital_county` | VARCHAR(100) | 医院所在县，全局筛选维度。 |
| `facility_name` | VARCHAR(255) | 医疗机构名称，全局筛选维度。 |
| `apr_mdc_code` | VARCHAR(30) | 第 1 层疾病系统编码，分组字段。 |
| `apr_mdc_description` | VARCHAR(500) | APR MDC 描述，仅展示使用。 |
| `apr_drg_code` | VARCHAR(30) | 第 2 层病例组编码，分组字段。 |
| `apr_drg_description` | VARCHAR(500) | APR DRG 描述，仅展示使用。 |
| `ccsr_diagnosis_code` | VARCHAR(30) | 第 3 层具体诊断编码，分组字段。 |
| `ccsr_diagnosis_description` | VARCHAR(500) | CCSR 诊断描述，仅展示使用。 |
| `discharge_count` | BIGINT | 该完整疾病路径的出院记录数。 |
| `length_of_stay_sum` | DECIMAL(20,2) | 住院天数总和。 |
| `total_charges_sum` | DECIMAL(26,2) | 总收费。 |
| `total_costs_sum` | DECIMAL(26,2) | 总成本。 |
| `updated_at` | TIMESTAMP | 汇总结果生成时间。 |

#### 3.1.4 支持软件

- MySQL Server 8.0
- Docker Compose
- MySQL 客户端或数据库管理工具
- Python MySQL 驱动

## 4. 结构设计

### 4.1 结构化数据库

#### 4.1.1 概念模型

系统以住院出院记录为事实主体。医院、患者人口学特征、入院信息、疾病编码、严重程度、支付方式、费用和离院去向都是该事实记录的分析维度。当前采用单张清洗后明细事实表，避免在原始去标识化数据中人为拆分后产生不必要的关联复杂度。

```text
医院维度 ─┐
患者维度 ─┤
入院维度 ─┤
疾病与风险维度 ─┼── inpatient_discharges（住院出院明细事实）
支付与费用维度 ─┤
离院去向维度 ─┘
                     |
                     v
          患者分析 / 医院运营 / 疾病风险 10 张 summary 表
```

#### 4.1.2 物理模型

```text
inpatient_discharges
    |
    +-- patient_age_gender_summary
    +-- patient_payment_summary
    +-- patient_disposition_summary
    +-- patient_admission_emergency_summary
    +-- patient_medical_surgical_summary
    +-- hospital_operation_summary
    +-- disease_system_summary
    +-- diagnosis_summary
    +-- patient_risk_summary
    `-- disease_drilldown_summary
```

`inpatient_discharges` 是清洗数据的唯一入库目标。各汇总表均为可再生数据，重新执行分析任务时必须从明细表重新计算和写入；不得将汇总表作为新的事实数据源再次汇总，以防止重复计数。

物理索引以数据追溯、增量去重和高频筛选为重点：

- `source_row_hash` 使用唯一索引，保障重复导入时的幂等性。
- `hospital_service_area`、`hospital_county`、`facility_name` 为核心筛选维度，应建立与查询场景匹配的联合索引。
- 汇总表应结合业务分组字段建立联合索引，例如支付方式、疾病编码、入院类型或医院名称。
- API 排名、分页和模糊匹配使用白名单排序字段与参数化 SQL，禁止直接拼接用户输入。

## 5. 运用设计（可选）

### 5.1 数据字典设计

数据字典包括数据类型、枚举值和计算口径。详细字段级说明以《数据库表结构说明.md》为准。

#### 5.1.1 数据类型

| 数据类型编码 | 数据类型 | 用途 |
|---|---|---|
| `DT_01` | BIGINT UNSIGNED | 明细主键、源行号和出院记录计数，满足大数据量增长。 |
| `DT_02` | VARCHAR(n) | 医院名称、区域、医疗编码、描述和分类字段；编码字段可保留前导零。 |
| `DT_03` | DECIMAL(p,2) | 住院天数、费用、成本、出生体重等需要固定精度的数值。 |
| `DT_04` | TINYINT UNSIGNED | 年龄排序、严重程度、死亡风险等小范围编码。 |
| `DT_05` | CHAR(1) | 急诊标记等单字符状态。 |
| `DT_06` | DATETIME/TIMESTAMP | 入库时间和汇总生成时间。 |
| `DT_07` | CHAR(64) | SHA-256 源行指纹。 |

#### 5.1.2 主要枚举与计算口径

| 字典编码 | 字段 | 取值/计算规则 |
|---|---|---|
| `DD_001` | `emergency_department_indicator` | `Y` 表示急诊，`N` 表示非急诊，空值表示未知；急诊占比的分母只统计 `Y` 或 `N`。 |
| `DD_002` | `zip_region_type` | `LOCAL`、`OUT_OF_STATE`、`UNKNOWN`。 |
| `DD_003` | `is_facility_redacted` | `1` 为医院信息脱敏占位记录，`0` 为正常记录。 |
| `DD_004` | `apr_severity_code` | 病情严重程度编码，正常范围 1 至 4。 |
| `DD_005` | `apr_risk_of_mortality` | 死亡风险编码，正常范围 1 至 4。 |
| `DD_006` | `apr_medical_surgical_description` | 标准分析值为 `Medical` 或 `Surgical`。 |
| `DD_007` | 金额差额 | `charge_cost_gap = total_charges - total_costs`。 |
| `DD_008` | 汇总平均值 | `SUM(指标总和) / SUM(discharge_count)`；筛选后不得对分组平均值直接再平均。 |

### 5.2 数据字典备注安全保密设计

1. 数据源为去标识化住院出院数据。系统不应尝试通过邮编、医院、疾病等组合反向识别个人，也不得将数据用于个体级决策。
2. 医院脱敏记录以 `is_facility_redacted` 标识。医院区域或县等关键上下文缺失、无法支持医院比较的记录，不应进入依赖医院空间维度的分析口径。
3. 数据库连接信息、数据库口令等敏感配置仅放在本地 `.env` 文件中；`.env` 和 `.env.*` 必须被 Git 忽略，`.env.example` 仅提供无密钥配置模板。
4. Flask 接口使用参数化 SQL、排序字段白名单和请求参数校验，防止 SQL 注入及无效分页、排序请求。
5. MySQL 生产部署应为应用账户分配最小权限，禁止应用使用 root 账户；开发环境默认口令不得迁移至生产环境。
6. 应定期备份 MySQL Docker 卷 `mysql_data`，并验证恢复流程。
7. 当前项目尚未实现登录、RBAC 和操作审计。若作为真实医疗生产系统上线，应补充 TLS、身份认证、角色权限、操作审计、密钥轮换、备份加密和数据保留/销毁制度。
